## v0.2.6

### 🐛 修复

- **WSL 监视器工具栏响应式** — 工具栏按钮在窗口变窄时自动换行，不再裁剪功能按钮
- **端口锁定机制** — 同一端口不能被多个监视器同时打开，避免冲突
- **端口占用状态实时刷新** — 连接/断开端口后自动更新所有监视器的端口列表

### 🔧 改进

- 端口下拉列表中被占用的端口显示 `[占用中]` 并灰显不可选
- 工具栏功能按钮区（自动滚动、时间戳、终端模式等）支持独立换行

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |
