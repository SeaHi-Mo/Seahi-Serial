## v0.5.6

修两个「一眼就看得见、但一直没人说清」的问题：**5 套深色主题的下拉菜单是半透明的** ——
面板底下的工具栏与日志区直接透出来（波特率列表里能看见「选择日志目录」那几个字）；
以及串口**设置波特率失败**时只回一句「参数错误」，用户根本不知道该改什么。

### 🐛 修复

- **深色主题下拉面板半透明**（浮世绘彩 / 诗意东方 / 水墨丹青 / 桃之夭夭 / 金风玉露 的**深色**版）：
  这几套主题里，本该只给「发送历史里当前选中项」的那层 15%~18% 主题色底，被**并进了下拉容器的
  选择器组**（`.sel-drop / .baud-dropdown / .send-hist / .send-as-drop`），于是整个面板被刷成半透明，
  同组那条 `box-shadow` 也跟着丢了。现在拆成两条：容器只留阴影、选中项才有底色。
  6 套浅色主题本来就是对的，未动
- **波特率设置失败只回一句「参数错误」**：Windows 对不支持的波特率回 `ERROR_INVALID_PARAMETER(87)`，
  `serialport` 把系统原文照搬上来 —— 板载 / 虚拟 COM1（通常只到 115200）上填 2000000 就会撞上，
  用户看不出是「这个串口不吃这个波特率」。现在直说：这个串口不接受 2000000 波特率，请换
  USB 转串口设备，或把波特率降到 115200 及以下（原始错误附在后面）

### 🧪 测试

- 前端无头断言 +1：**下拉面板容器的底色只许写不透明的 `var(--surface-1)`** —— 写 rgba 半透明色
  就直接挂（把这次的写法放回去，这条立刻变红）
## v0.5.5

修一个"打开了就关不掉"的界面 bug：**只读（沙箱）模式**开启后，弹窗里那颗开关被永久置灰 ——
而它恰恰是关掉只读模式的**唯一入口**（只读模式下 AI 连 `mcp_config_set` 都会被拒，这是故意的）。

> 已经卡在只读模式里的用户：装上这一版，点一下那颗按钮就能关掉（不必手改 `ai-config.json`）。

### 🐛 修复

- **只读模式开了之后无法关闭**：`mcpToggleReadOnly()` 为了挡住连点先把按钮置灰，而状态回来时
  `renderMcpStatus()` 只重写了文案、**从来没有把 `disabled` 放回来** → 开完只读模式，这颗按钮就
  永远点不动了（`.ble-modal-btn:disabled` 只有 `opacity:.45`，肉眼几乎看不出差别，表现就是
  "点了没反应"），此后只能重启应用或手改配置文件。现在禁用态只认独立的在途标志
  `_mcpReadOnlyBusy`，成功与失败**两条路**都会解禁 —— 一次后端抖动也不能把按钮锁死
- 这个开关补了 **17 条断言**（含"开完必须解禁 / 再点真的能关掉 / 命令失败也要解禁"等行为断言）。
  拿修复前的代码跑，这 17 条里正好挂 7 条

### 📚 文档

- `doc/MCP_DESIGN.md` §17 记录本次故障的根因，以及"断言集当时为什么没守住"
  （当时只有"按钮存在""文案跟着状态"两条源码扫描，恰好没测"还点不点得动"）

## v0.5.4

本版把**蓝牙（BLE）**面板接进了 AI（MCP）：从"只能靠通用界面桥一个个点控件"变成**说人话就能操作**
（33 → **49** 个工具）；并修掉一类"工具写了、但客户端收不到参数/看不到结果"的真问题。
顺带让 WSL 串口起不来时能说清原因。

### ✨ 新增

**BLE 语义工具（15 个）+ 危险动作清单（1 个）**

| 干什么 | 工具 |
|---|---|
| 扫描与列表 | `ble_start_scan` / `ble_stop_scan` / `ble_list_devices`（**支持分页** `limit`/`offset`，返回 `hasMore`/`nextOffset`） |
| 连接 | `ble_connect`（列表里有就点卡片连；**列表里没有就按 MAC 直连** —— 设备不广播时只有这条路；不给 MAC 就用当前选中的那台）/ `ble_disconnect` |
| 特征读写 | `ble_get_services` / `ble_read` / `ble_write` / `ble_subscribe` |
| 数据与信号 | `ble_get_output`（本次会话收到的通知/读回的内容）/ `ble_refresh_rssi` |
| 从机（本机当外设） | `ble_periph_status` / `ble_periph_start` / `ble_periph_stop` |
| 状态 | `ble_get_state`（不碰适配器就知道"扫没扫、连没连、订阅了几路"） |

- **每个工具都复用面板那颗按钮的代码路径**（不給 AI 另写一套），所以界面上的日志、图标状态、写入窗会跟着变
- **危险动作要二次确认**：`ble_periph_start`（对外广播）等不带 `confirm:true` **不会执行**，
  返回 `-32006` 并说明后果；`mcp_danger` 可以先查清单

**AI 动手时界面会跟着走**：动哪个面板的控件，就先把那个面板切出来（BLE 的动手类工具、`ui_set`/`ui_click` 都算）；
纯读状态**不切页**（免得客户端一轮询就把你从别的页面拽走）。

**扫描结果的三种读法**（读的是同一份数据）

- 有语义工具：`ble_list_devices`（每台带 **MAC / 名称 / RSSI / 是否配对 / 是否选中**，支持分页翻完全部）
- 只有通用界面桥：`ui_get_state` + `{"section":"bleDevices"}`
- 只想看文本：文本摘要**一行一台** —— `共 51 台（第 1-20 台）：5F:90:… -80dBm | …（下一页 offset=20）`

**新增工具自检脚本**（`node .walkthrough/mcp_smoke.js`）：对着**正在运行的应用**把 49 个工具逐个真调一遍
并打印结果 —— 一眼看出"客户端为什么看不到新工具"（十有八九是跑的是旧构建，工具定义是编译进 exe 的）。

### 🐛 修复

- **回执那一跳没等 Promise**：连设备 / 读写特征 / 从机启停这些分支返回的是 Promise，回执却直接读
  `res.ok` → 客户端只看到一句没头没尾的"前端执行失败"（真机实测 `ble_periph_status` 就是这样）。
  现在真实原因会原样带给 AI
- **`ble_read` / `ble_subscribe` 的 `char` 根本没到前端**（只做了校验、忘了放进 payload）——
  真机上这两个工具会一直回"要给 char"
- **扫描结果读的是面板缓存**（每 2 秒才轮询一次）：刚开完扫描立刻问会读到空列表。现在每次现问一次后端
- **扫描结果"只有数量、没有 MAC/名称"**：通用渲染只展开 3 个元素，45 台看着像"只回了数量"。
  现在设备表按一行一台输出，通用数组也改成按字数预算展开
- **订阅状态判断大小写敏感**：`ble_subscribe` 用调用方给的 UUID 去查订阅表查不到 → **退订会静默不生效**；
  改用服务树里的真实 UUID
- **特征图标要先手动展开服务才在 DOM 里** → AI 会误报"特征不支持"；现在会先展开拥有它的那个服务
- **`ui_get_state` 的 `limit`/`offset` 没转发到前端** → 通用桥的分页静默失效
- **WSL 串口"bridge 启动超时"查不到原因**：现在分开报"进程已退出 / 真的超时"，并带上 stderr 最后几行
  与可执行提示（缺 python3 / 脚本没落到 /tmp / dialout 组不存在）
- **一页速查表把危险写工具标成「读」**（`ble_periph_start` / `ble_periph_stop`）
- 清掉 6 条 `dead_code` 编译警告；`report::guard` 真正用起来（生成控件工具定义时兜住 panic）

### 📚 文档

- [`doc/MCP.md`](doc/MCP.md) 新增「同一份数据的多条入口（该用哪条）」与「看着像重复、其实不是」两张对照表
- [`doc/MCP_TOOLS.md`](doc/MCP_TOOLS.md) 重生成：49 个工具，每个都带返回结构、上限与注意事项
- `AGENTS.md` 补上"改了 MCP 工具后要跑哪些检查"（含对运行中的应用跑自检）

## v0.5.3

本版把**快速指令**这一栏重新做了一遍：从"一个扁平列表 + 一个循环开关"升级成**循环组**，
并把 AI（MCP）能做的事补齐到与面板一致；顺带修掉一个"导入明明成功却报失败"的老 bug。

### ✨ 新增

**循环组：一组一张表**

- 一个监视器可以有多组，每组有**可重命名的抬头**（拖动握把 · 参与开关 · 组名 · 折叠箭头 · 条数 · 删组）；
  `＋ 新建循环组` 追加到最下面并默认带 1 条空指令，首次启动默认 1 组，最后一组删不掉
- **循环顺序 = 组的上下顺序 → 组内顺序号**：把哪一组拖到最上面，循环就从它开始；走完最后一组回到第一组。
  拖动换位带滑动动画（FLIP），松手即写回文件
- **每组的"参与开关"**（滑动开关）：关掉 = 整组跳过，文件里的顺序号一格都不动
- **每组自带一张表的表头**（顺序 / 指令 / 延时(ms) / HEX），`＋ 添加` 在这一栏最右 —— 面板与文件同形
- 两个一眼可辨的蓝色标识：顺序号 `>0` 的方框**蓝底**（= 参与循环）、组**展开**时折叠箭头**蓝底**

**外部文件：一组一张表 + 格式文档**

- 文件里**每个 `## 组名` 抬头 + 下面那张表 = 一个循环组**（**两个及以上 `#`**；单个 `#` 仍是注释）；
  没有抬头的老文件读成一组，文件一字不动
- 新增 [`doc/QUICK_CMDS.md`](doc/QUICK_CMDS.md)：列名与别名表、分组抬头、注释与文件头、
  写回规则、上限、FAQ —— 照着就能手写一份指令文件

**MCP：`serial_quick_cmd` 补齐**

- 新增 `action`：`loop`（开/关循环发送）、`add` / `update` / `remove`（增删改一条）、
  `group`（`add`/`remove`/`rename`/`move`/`on`/`fold` 组操作）
- 列表更全：每条带所属组与 `domIds`（可直接交给 `ui_set`），另给 `groups[]`（含是否参与循环/是否折叠）与 `loop{on, planLength}`
- 每个动作都**复用面板那颗按钮走的同一条代码路径**，不给 AI 另开一套

### 🐛 修复

- **导入成功却提示"导入失败: TypeError ... (reading 'length')"**：组模型上线后
  `quickCmds` 变成 null，而成功提示里还在读它的 `.length` —— 这句在 `.then` 里抛异常，
  被 `.catch` 兜住显示成失败。现在条数走组模型的口径，且"成功后不许出现导入失败"进了断言
- **导出物与面板一一对应**：列序改成 `顺序号 │ 指令 │ 延时(ms) │ HEX`（原来顺序号排到了指令右边）、
  面板有几条就写几行（空条目也占一行，不再少写）、不再多出一列「名称」、HEX 列写成布尔 `true`/`false`
- **拖动组排序没有动画**：原来每次换位都整块重建列表（DOM 被替换 → 硬跳）；改成只挪节点 + FLIP 过渡
- **折叠一组时列标题没收起来**：只有数据行消失、表头孤零零留着，现在一起收
- 分栏最小宽度 120px → **300px**（= 缺省宽度）：一条六格在更窄的栏里会挤成一团
- 导入成功 / 重载成功不再弹提示（列表本身就是反馈；失败仍然弹）

### 🎨 排版

- 组抬头：**拖动握把最左** → 参与开关 → 组名 → 折叠箭头（紧挨"N 条"）；`＋添加` 挪到该组表头行最右并改用主题色
- 折叠箭头与拖动握把改成 **CSS 画的**（原来用 `▾` / `⠿` 字形，小字号下几乎看不见）
- 面板里所有横向分隔线**等长且左端顶到面板左缘**（逐像素量出来的），列标题一行与数据行逐列对齐
- 标题行控件**分两端**：左 = 循环发送开关（状态），右 = 新建循环组 / 导入 / 导出（动作）

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐，含 ADB platform-tools） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |

## v0.5.2

本版把社区贡献的**窗口位置与大小记忆**正式发出来（v0.5.1 的安装包里还没有它），并修掉
它在老用户升级与异常启动两种情况下会露出的问题。

### ✨ 新增

**窗口位置与大小会被记住**（社区贡献 PR #20，感谢 @Tom Yin）

- 退出时把主窗口的**位置、尺寸、最大化状态**写进 `%APPDATA%\seahi-serial\window.json`，
  下次启动回到原处；多显示器、副屏在主屏左侧（负坐标）都支持
- 拖动/缩放时 **400ms 去抖**自动保存，退出瞬间再强制存一次 —— 关窗前最后那一下缩放不会被丢掉
- **异常状态不会污染记录**：启动期窗口隐藏时摆放出的瞬时尺寸（实测有 2068×2060 这类值）、
  最小化时系统给的 `-32000` 哨兵坐标都不写盘；最大化只记标志，取消最大化后回到最大化
  之前的那个普通尺寸（不会把全屏尺寸当成普通尺寸存下来）
- **拔掉外接显示器也不会丢窗口**：恢复位置前先判断目标矩形能否与某块显示器重叠到可操作
  尺寸（至少 60×40），不满足就留在居中的默认位置，而不是被"放"到不存在的虚拟屏上
- 主窗口改为**先恢复几何、再一次性显示**（启动时隐藏 + 页面就绪后 `reveal_main_window`），
  不再看到"先按默认尺寸闪一下、再跳成上次的大小"

### 🐛 修复

- **老版本升级后窗口尺寸被重置一次**：0.5.1 及以前尺寸存在 `config.json` 的
  `windowWidth`/`windowHeight` 里，本版改由 `window.json` 统一管。现在读不到新文件时会
  回退用旧字段，升级后第一次启动尺寸不变；旧字段里没有位置，此时**保持居中**
  （若照搬 `(0,0)` 会把窗口顶到左上角）
- **极端情况下「应用启动了但看不到窗口」**：界面初始化失败或 8 秒看门狗超时走到兜底
  错误页时，窗口本身还是隐藏的 —— 连那个错误页用户都看不见。现在兜底页会强制把窗口
  显示出来，后端另有 4 秒兜底显示，两条路都堵上了
- `package-lock.json` 的版本号从 0.4.0 更正为与主版本一致（不影响运行，纯版本记录）

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐，含 ADB platform-tools） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |

## v0.5.1

### ✨ 新增

**快速指令大改：从"发送栏角落的图标"变成「监控输出区右侧的可折叠分栏」**

- **入口重做**（可用性研究里 F2/P-09：原来那个无文字三横线图标 ≥50% 用户找不到）：
  分栏贴在输出区右侧，**只占输出区那一行的高度**，不跨越上方工具栏与下方发送栏；
  展开只挤输出区，两栏仍占满窗格宽度
- **默认折叠**：折叠态只有一条 14px 指示条 —— **没有图标、没有文字、没有小三角**，
  只有一根居中的细握把（悬停点亮）
- **展开态整条填主题蓝**（悬停再亮一档），蓝底上握把转成白色；配色全部走主题变量，
  12 套主题（含浅色）都成立
- **宽度可拖**：展开后按住折叠条左右拖动即可调宽（120–640px，且永远给输出区留 160px），
  拖动时不做过渡、跟手；宽度按监视器写入 `config.json`，重启沿用
- 列表改**两行卡片式**排版（名称行 + 指令内容独占一行），长 AT 指令看得全；配细滚动条

**快速指令支持外部文件（导入 / 导出 / 写回）—— 文件就是列表的存储**

- 标题行右侧三个按钮：**＋添加 / 导入 / 导出**；导入后来源行显示文件名 + 重载 + 断开
- **文件即存储**：导入后，面板里的增/删/改都会**写回该文件**（去抖 0.6s、原子写），
  不再有"改了半天其实没落在文件里"的两份真相
- **三种载体自动识别、写回保持原样**：Markdown 表格 `| 名称 | 指令 |`、TSV、纯指令行
  （一行一条，名称取指令本身）。**刻意不按逗号切分** —— AT 指令里逗号是常态
  （`AT+CWJAP="ssid","pass"`），按 CSV 切必然把一条指令切碎；文件叫 `.csv` 也按"整行一条"读
- **手写文件不会被改坏**：注释、空行、表头、`|---|` 分隔行、第 3 列起的备注都按原位置保留；
  原本只写一列的行不会被"补"成两列；坏行/超限行跳过并给出行号与原因（能部分加载就部分加载）
- **YAML / TOML 文件头（front matter）原样保留**：`---` / `+++` 包起来的头部一字不动，
  不会被当成指令（不识别它的话 `baud: 115200` 会变成一条"指令"、写回时还会被转成表格行）；
  其中 `baud`/`mode`/`lineEnding`/`delay`/`expect`/`timeout`/`hex` 会明确提示**暂不生效**
  （发送沿用面板设置），纯描述性的 `title`/`note` 不打扰
- **不会被别的程序改动覆盖**：写回前比对内容哈希，文件被编辑器改过就报冲突并提示先「重载」；
  **只有成功读过一次（手里有哈希）的挂载才允许写回**，文件被删/读不到时宁可拒写，
  也绝不拿内存里的列表把用户文件重写一遍（两端各拦一道）
- **编码沿用读入时的**：UTF-8（含 BOM）→ 失败回退 GBK，写回仍用原编码，不会把用户的
  GBK 文件变成乱码；上限：文件 256 KB、500 条、名称 64 字符、单条指令 4096 字符（`mcp_limits` 里可查）

### 🐛 修复

- **重装/升级时因 adb 占用导致的一连串安装错误** — `adb` 启动的服务器是**常驻后台进程**
  （应用退出后依然活着），它把 `{app}\platform-tools` 下的 `adb.exe` / `AdbWinApi.dll` /
  `AdbWinUsbApi.dll` 全映射住：运行中的 .exe 无法就地覆写、已加载的 DLL 连删除都不允许。
  原来 `[Files]` 用 `ignoreversion` 无条件重写全部 14 个文件，于是重试 4 次后弹
  「尝试复制下列文件时出错」；同 AppId 升级时旧版卸载器也删不掉 `{app}`。现在安装器在
  `PrepareToInstall` / `ssInstall` / `usUninstall` 三处**只结束镜像路径位于本应用 `platform-tools`
  下**的 adb（不误伤 Android Studio 等其它来源），并改用 `replacesameversion` +
  `restartreplace`/`uninsrestartdelete` 兜底
- **安装包"外壳新、内核旧"** — 主程序版本由 `env!("CARGO_PKG_VERSION")` 在**编译期**烘焙，
  只改版本号文件不重编，`target/release/seahi-serial.exe` 里还是旧版本（曾出现"安装包 0.5.0、
  装出来的应用 0.4.0"）。现在 `installer.iss` 顶部有 ISPP 编译期守卫：主程序 exe 版本 ≠
  `MyAppVersion` 直接 `#error`，并打印实际版本；安装包自身的"文件版本"字段也补上了
- **未装 usbipd 时自动勾选勾错了对象** — 原来用「任务列表最后一项」当 usbipd 任务，
  新增 ADB PATH 任务后它指到了 ADB 上；改为显式索引常量（加任务时会一起看）
- **ADB PATH 条目的重复/残留** — 比较时归一化尾斜杠与大小写、无变化不写注册表、
  路径含单引号时安全转义

### 🔧 改进

- **BLE 页左侧设备栏不再那么宽**：默认从固定 400px（默认窗口的 38%）收到 **288px**，
  并把 GATT 详情让出来；两侧现在可以**拖手柄调宽**（主机/从机两种模式共用同一宽度、
  切换不横向跳动），宽度随配置保留
- **MAC 直连输入框**占位符改为「输入目标设备 MAC 直接连接」（原文案太长）
- **MCP**：`serial_quick_cmd` 的列表返回新增 `source`（`file`/`config`）与 `file` 路径 ——
  让 AI 知道这个列表来自外部文件（面板里改动会写回文件），而不是"改配置就能保住"；
  `mcp_limits` 补上快速指令文件的相关上限

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐，含 ADB platform-tools） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |

## v0.5.0

### ✨ 新增

**MCP 支持 —— 让 AI 客户端直接操控本程序**

内置一个 MCP（Model Context Protocol）服务器：跑在应用进程内、**只监听本机回环地址**、**只用 SSE**，随程序启动。

- **标题栏新增 MCP 图标**（在「风格」左边，与「风格」图标同色，带状态点：灰=未启用 / 绿=监听中 / 蓝=有会话 / 红=启动失败）。点开弹窗即可**一键开启/关闭**（同一个按钮，点一下开、再点一下关）、查看当前连接 URL、一键复制**客户端配置**或**安装提示词**。版本 / 工具数 / 请求数 / 丢弃数可以在状态文案上悬停查看。
- **随程序启动、端口自动回退**：默认 `127.0.0.1:7777`，被占用会自动向后找（最多 20 个），实际端口在弹窗里显示。
- **访问令牌**：首次启动自动生成；token 放在 URL 里（兼容不能自定义请求头的 SSE 客户端）。弹窗可「重置令牌」，旧令牌立即失效。
- **端点发现文件** `%APPDATA%\seahi-serial\mcp-endpoint.json`，供下面的安装器与外部工具读取。
- **33 个内置工具**：13 个**串口语义工具**（选端口/设波特率/帧格式/DTR-RTS/开监控/发数据/**读回监控数据**/清空/发送历史/快速指令，还带分栏 `pane` 参数）+ 20 个通用工具，四类：
  - 应用/服务器：`app_info`、`mcp_status`、`mcp_limits`、`serial_list_ports`
  - **界面操作**：`ui_list`（枚举界面上所有可操控控件，含"为什么不可用"）、`ui_describe`、`ui_get`、`ui_set`、`ui_click`、`ui_get_state`
  - **日志**：`log_channels`、`log_tail`（可按序号增量拉取）、`log_search`（子串/正则、可跨通道）、`log_stats`、`log_clear`、`log_export`
  - 记录与配置：`mcp_calls`、`mcp_stats`、`mcp_config_get`、`mcp_config_set`
- **界面操作会真的同步到界面**：工具执行走的是与用户点击**完全相同**的路径（合成 DOM 事件），界面必然跟着变，不存在"AI 改了但其实没生效"。
- **可选：每个控件一个工具**（`ctl_xxx`，默认关闭）。打开 `expose.autoControlTools` 后，界面上每个按钮/输入框/下拉都会生成独立工具；默认关闭是为了不拖累 AI 选工具的准确率。
- **日志中心**：后端统一收集（应用日志、错误、BLE 通知、串口收发、界面提示），每通道有字节上限与**丢弃计数**，会明确告诉你"曾丢过多少条"，不会让你以为日志是完整的。
- **内存上限是被"执行"的，不只是被报告的**：日志总量硬上限 16 MiB（超了自动裁掉最大通道的旧日志，**回收次数与回收字节数都能查**）、通道数上限 64（通道名会随监视器数量增长，到顶后不再新建并记账）、单条 8 KiB 截断；这些都不在写入路径上阻塞串口读线程（拿不到锁就丢弃并计数）。
- **AI 调用记录**：每次工具调用写一行到 `%APPDATA%\seahi-serial\ai-calls.jsonl`（时间、会话、工具、参数、成败、耗时、**改动了哪些控件**），按大小自动轮转。
- **配套 npm 安装器** `npx seahi-serial-mcp install`：把端点写进各客户端配置；端口回退后重跑一次即可修正。零依赖、无 postinstall、**写前会确认程序在运行**、改前先备份、遇到含注释的配置文件会拒绝硬改并给你可粘贴片段。
- **工具列表变化会主动通知客户端**：界面控件增减 / 可用状态变化时，服务器向所有已连接会话推送 `notifications/tools/list_changed`（仅在真的变了且服务器在运行时发），客户端不需要轮询或重连。哨兵端点 `GET /healthz` 仍然**只回 `{"ok":true}`**，不泄露版本等任何信息；要看详情得走 `GET /status` 并带 token，且**详情里 token 与完整 URL 也一律打码**。

**安全边界**：仅监听回环（不允许配置成对外开放）、必须带 token、工具不能修改 token、**AI 记录与用户配置严格分文件**（`config.json` 里不会出现任何 AI 痕迹，有专门断言守着）。

- **AI 调用效率**：握手时通过 `initialize.instructions` 一次性下发工作方式（先看状态 → 串口主流程 → 错误码语义 → 各项上限），让 Agent **第一次就做对**，而不是靠失败去猜；错误信息里直接给出可选值/下一步该调哪个工具。
- **只读（沙箱）模式**：弹窗里一键打开后，AI **只能读、不能改** —— 所有写操作（`ui_set`/`ui_click`/`serial_open`/`serial_send`/`log_clear`/`mcp_config_set`…）一律被拒并返回 `-32007`，**界面与配置文件一个字都不变**。适合"先让 AI 自由探索、确认无误再放开"。注意这个开关**只能由你在界面上关**（`mcp_config_set` 自己也是写工具，AI 打开后就关不掉）—— 这个不对称是故意的。

### 🐛 修复

**三个"用久了就出问题"的既有缺陷（都与内存 / 磁盘无限增长有关）**

- **调试日志会一直涨到把磁盘写满** — `%TEMP%\seahi-serial-debug.log` 原来没有任何上限（每次调用追加、从不轮转，Release 版也一直写）。现在单文件限制 **4 MiB**，超过即轮转到 `.1`（只保留一份备份）；另可用环境变量 `SEAHI_DEBUG_LOG=0` 完全关闭它
- **会话日志缓存只限个数、不限大小** — `%APPDATA%\seahi-serial\log-cache\` 原来只保留最近 10 个文件，但**单个文件没有上限**：按 921600 波特估算，单会话跑 1 小时约 330 MB，最坏能到 GB 级。现在单文件上限 **8 MiB**、目录总预算 **64 MiB**；触顶时写入一行终止标记，并**在界面明确提示**（不再静默停止写入）。同时清理旧缓存时会跳过正在写入的文件，避免把日志从活跃会话脚下抽走
- **长时间收数据后界面内存持续上涨** — 三个原因叠加，一起修掉：
  1. 紧凑缓冲区要攒到 **100 万行**才裁剪（裁完还剩 50 万行）→ 改为**按字节预算**（8 MiB），超出即裁到一半
  2. 数据缓冲区只做 2 倍扩容、**裁剪后从不收缩**，扩容瞬间还要同时持有「旧 + 新」两份 → 现在容量会跟随实际用量收缩
  3. 输出区 DOM 在「用户停在顶部看历史」或「有选区」时**整个跳过**清理，行数可无上限增长（每行是真实 DOM 元素，比字节缓冲贵得多）→ 改为「**延后 + 硬上限**」：受保护时也保证不超过 6 万行，并做滚动补偿让视觉位置保持不动；选区消失后自动补裁到常规上限，而不是永远不裁
  4. 每个监视器**一创建就预分配 1.7 MB**（1 MB + 3×10 万槽位），哪怕它一直空着 —— 实测两个监视器就占掉 **3.5 MB** 的 Typed arrays。现在改为 **64 KB 起步、按需翻倍**（空闲监视器降到约 **92 KB**），并在「清空输出」时把容量收回去

- **顺带修掉两个隐藏 bug**：
  - 紧凑存储的**索引数组扩容时没拷贝旧数据**（只 new 一个更大的数组就替换），一旦触发会把已积累的行偏移表清零，导致**保存 / 复制 / 历史加载读到错乱内容**。原来要 10 万行才踩到，这次把起始容量调小后会变成 4096 行就爆 —— 已在扩容时补上拷贝
  - 两条 WSL 监视器创建路径**漏设了字节预算字段**，会让 WSL 分栏的「保存 / 复制全部」只输出最后一行 —— 已补齐，并给预算判断加了默认值防御

**MCP 侧：用官方 MCP SDK 对真实运行的实例逐个实调，修掉四类问题（发布前）**

- **错误码分流曾经整条失效** —— 前端回执时丢了一个标记字段，于是"控件路径 / 取值不存在"统统返回 `-32006`（"没有界面上下文"）而不是 `-32602`。对 AI 的实际后果是**误判方向**：它会以为"应用没开界面"而反复重试，而正确动作是照着错误里给的可选值改参数。现已修正，并补上**把"回执 → 桥 → 错误码"整条链测穿**的测试（以前两端各测一半，中间那段没人管）
- **只给一个目标的写操作，失败时仍回"成功"** —— `ui_click` / `ui_set{path}` 原来把失败藏在逐项结果里、顶层依旧是成功，调用方（和人）会以为点上了。现在**单目标失败 = 整次调用失败**（多目标才保留"逐条回报"的批量语义）；整批里已生效的字段也会在错误信息里点名，避免误判"界面没动"
- **端口查询的返回里看不出端口名** —— 文本摘要遇到数组只写 `ports=1 项`，而**很多客户端只把这段文本给模型看**，人也是先看这行。现在摘要会真的列出内容；同时把返回字段统一成小驼峰（`portName` / `friendlyName` / `productName`，旧的下划线拼写仍然兼容）
- **界面上的"会话"数字不刷新** —— 客户端明明连上了，弹窗还显示 0 会话。服务器侧一直是对的（`sessions=1`），错在**会话增减时没有通知界面**（会话是在传输层增删的，而推送只在启动/启停时发生）。现在每次连接与断开都会推送，并新增 `mcp_status.statusEmits` 让这件事**可观测**，不再出现"界面不刷新但查不出原因"
- **被限流时那次调用会"挂到超时"** —— 限流回包的请求 id 原来是 `null`，客户端配不上号，于是那次调用一路等到超时，客户端以为失败又重试，越限流越糟。现在回包带上**本次请求的真实 id**，并说清"本次调用被拒（没有执行）+ 怎么放慢"
- **没有设备时"开始监控"要白等 6 秒** —— 机器上一个串口都没有时，原来照样点按钮再等轮询超时。现在**立刻**返回明确原因（插上设备后再试），不再白等
- **工具 schema 的可移植性**（由**官方 MCP Inspector** 的 portability 检查发现，顺便也接上了这个第三方工具）—— `ui_set` 的参数 schema 原来是空 `{}`（等于不约束任何东西），补上类型后又发现 `type` 写成数组会让部分客户端拒收工具或丢掉约束，最终改用 `anyOf`。现在 33 个工具的 schema 检查**零告警**，并把这两条判据固化成了不变式，以后写错会直接测挂
- **"看得到改不了"的开关** —— `serial_get_state` 会报出「更多设置」栏的展开状态（`advOpen`），但 `serial_set_display` 设不了它（错误提示里也没提）。现已补齐，并加了一条断言守着"报出来的每个开关都必须设得了"

### 📝 说明

- **本版包含两块内容**：① MCP 支持（新功能，**随程序启动自动监听、只监听回环、必须带 token**；不想要就在弹窗里一键关掉）② 三个"用久了就出问题"的既有缺陷修复。两者互不依赖，可以只当修 bug 的版本升级。
- **本版新增的串口语义工具**：MCP 不再只能"按控件路径操作"，而是有了 `serial_get_state` / `serial_select_port` / `serial_set_baud` / `serial_set_frame` / `serial_set_lines` / `serial_set_display` / `serial_open` / `serial_close` / `serial_send` / `serial_clear` / `serial_get_history` / `serial_get_output` / `serial_quick_cmd`（共 13 个，工具总数 **33**）—— AI 可以直接说"打开 COM3、115200、发 AT、看设备回了什么"，不必拼控件路径。
- **已知未完成**（如实说明）：BLE / ADB / WSL 三个面板的语义工具尚未提供（目前只能用通用控件桥操作）；串口的**收发链路**（真的连上、真的发出去、数据真的进了日志通道）在开发机上**没有串口设备可验**，未经真机验证。
- MCP 服务器**随程序启动就开始监听**（默认 `127.0.0.1:7777`，被占用会自动向后找并显示实际端口）。在标题栏点 MCP 图标，弹窗里**同一个按钮点一下关、再点一下开**；关闭后不占端口，日志中心也随之关闭。
- 详细的连接方法、可用工具清单、排错步骤见 **[`doc/MCP.md`](doc/MCP.md)**；设计与取舍见 [`doc/MCP_DESIGN.md`](doc/MCP_DESIGN.md)。
- 输出区的常规裁剪上限（1 万行 / 1.5 万行软上限）与「滚回底部时清理」的行为**保持不变**；变化只发生在原来会无限增长的那些场景。
- ⚠️ **一处行为变化值得注意**：界面「复制 / 导出全部」读的是内存里的紧凑存储，它的上限从「100 万行」改成了「**8 MiB**（约 20 万行）」。**文本超过 8 MiB 的超长会话，最旧的部分会被裁掉，导出内容不再包含全部历史**；而磁盘上的会话缓存文件保留的则是**开头**部分（单文件上限 8 MiB）—— 两处是从相反方向截断的。取舍分析与后续可选项记录在 `doc/MCP_DESIGN.md`（§13 D14）。

## v0.4.1

### ✨ 新增

**蓝牙主机 —— 搜不到 / 连不上的几个真实痛点**

- **按 MAC 直连** — 从机一旦被 Windows 配对过、或被别的手机连走，往往就不再广播，于是永远进不了扫描列表、也就永远选不中。现在可以在设备列表上方直接填地址连接（回车即连），**完全不依赖广播**（底层用上了 btleplug 的 `add_peripheral`）
- **显示协商后的 MTU** — 连接后详情头显示 `MTU 185（载荷 182）`。写长数据失败时，终于能分辨是「MTU 不够」还是「特征不对」
- **扫描时长可选** — 5 / 15 / 30 / 60 秒或「持续」，默认 **15 秒**（原来硬编码 5 秒：低功耗从机广播间隔常在 1~2 秒甚至更长，5 秒窗口经常一包都收不到）；到点自动停止时会**明确提示**，不再悄无声息
- **支持多个蓝牙适配器** — 扫描、设备列表、信号强度、按地址查找全部覆盖所有适配器，同一台设备被多个适配器听到时自动去重（原来只用第一个，插了 USB 蓝牙棒也没用）

### 🐛 修复

- **后端连接没有超时** — 前端 15 秒放弃后，后端若稍后才连上，就会出现「界面显示未连接、实际已连接」的长期错位。现在后端有 10 秒显式超时，超时会主动断开把状态收敛掉
- **通知丢数据无人知晓** — 轮询从 400ms 收紧到 250ms、缓冲从 500 提到 2000，并且**溢出的条数会明确写进日志**（以前静默丢最旧的，会让人以为设备本来就发那么少）
- **蓝牙页左栏宽度在主机 / 从机之间不一致** —— 切换模式时布局会横向跳动，现已统一
- **从机面板滚动条是浏览器默认样式** —— 深色主题下又宽又亮很扎眼，现已与日志区统一

### 📝 说明

- 本版新增了 **BLE 从机（GATT Server）** 的完整能力（服务 / 特征配置、接收主机读写、主动下发通知、广播配置表格导入导出、多套配置保存），但 **界面入口默认隐藏**：
  在本机（Intel 集成蓝牙）上实测**无法广播** —— 射频能发广播，但带「服务 UUID / 广播名」的广播帧会被系统拒绝（`E_INVALIDARG`），
  而 GATT 服务必须广播服务 UUID，因此恒为 `Aborted`。完整证据链与结论见 `doc/BLE_PERIPHERAL.md` 第 5 节。
  代码整体保留：在允许该广播的适配器上，把 `src/index.html` 里的 `BLE_PERIPH_MODE_ENABLED` 改为 `true` 即可启用。

## v0.4.0

### ✨ 新增

**蓝牙(BLE) 调试**

- **设备扫描与列表** — 扫描附近 BLE 设备（名称 / 地址 / 地址类型 / 信号强度 / 广播内容），按信号强度排序；扫描中明确提示「正在扫描…」，支持按名称或 MAC 过滤，过滤词与展开状态随配置保留
- **设备类型识别与图标** — 依据厂商 Company ID 与 AD type 自动识别并显示图标：标准 BLE / Apple(含 iPhone) / PC(微软) / iBeacon / Bluetooth Mesh；iBeacon 与 Mesh 在设备名后附灰色文字提示（如 `NexBeacon Pro (iBeacon)`），避免与同为 Apple 厂商的 iPhone 混淆
- **广播内容解析** — 展示原始广播字节并按 Bluetooth SIG AD Type 切分；解析广播名、广播服务、外观、发射功率、厂商数据（含 iBeacon/Eddystone 等）、服务数据
- **GATT 服务 / 特征树** — 连接后展示完整服务与特征树；服务按标准名标注，识别不到类型的统一标 `Custom Service`；特征支持**读取 / 写入 / 订阅通知与指示**
- **特征描述符可读写** — `0x2902 CCCD`、`0x2901 User Description` 等描述符不再只是标签：按 GATT 规范提供读取与写入（CCCD 可手动开关通知），读取结果人性化显示（0x2901 按文本、CCCD 按「通知/指示是否启用」）
- **写入（发送）窗口** — 可选**写响应 / 无响应**、**HEX / 文本**、**行尾（无 / CR / LF / CRLF，与串口监视器一致）**；支持 `\r \n \t` 转义；窗口可拖动调整大小，输入区为多行文本框；回车发送、发送后自动清空
- **配对支持** — 连接需要配对的设备时，由应用发起 WinRT 配对并弹出窗口显示/输入**配对码**，等待确认（60 秒无响应按取消）；配对成功后自动重连
- **数据日志** — 通知按「**文本 + 灰色十六进制**」显示，并标明来源（服务 · 特征）；切换设备或断开时清空，切页保留
- **蓝牙页内嵌串口监视器** — 顶栏「打开额外监视器」在蓝牙页作为**开关**：在页面右侧打开一个串口监视器抓设备日志（最多一个），宽度可拖动调节；是否打开、宽度与端口设置随配置保留

### 🐛 修复

- **重连后找不到设备** — 断开后 btleplug 会把设备移出内部表；现保留外设对象直接重连（设备停止广播时也能重连），并支持短扫描兜底
- **重连后写入报「该对象已经关闭」** — 重连会替换底层设备对象但旧 GATT 缓存未清，导致写入/订阅拿到已关闭对象；已在 vendored fork 中于 `connect()` 清理缓存（详见 `src-tauri/vendor/btleplug/VENDOR.md`）
- **切到其它页面一段时间后连接被系统回收** — WinRT 的 `GattSession.MaintainConnection` 默认为 `false`，空闲即回收链路（实测离开 150 秒必断）；fork 中置为 `true`
- **设备主动断开无感知** — 信号强度轮询顺带回报链路状态，设备侧断开后约 3 秒内复位为未连接并提示
- **订阅状态残留** — 断开后订阅图标仍显示「启用」；现随断开复位
- **已连接标记串台** — 换连另一台后旧设备仍显示「已连接」；现按地址清理
- **切页回来右侧详情空白** — 已连接设备若不再广播就不在扫描结果中，详情将找不到设备；现补进列表
- **查看未连接设备时串台** — 右侧曾显示「另一台已连接设备」的 GATT 服务树；现只在查看已连接设备时使用真实服务树，否则用该设备广播里的服务
- **配对流程干扰正常连接** — 早期实现对所有连接失败都尝试配对，而配对仪式会占用设备、打断链路；现仅在错误确为「认证/加密不足」时才发起
- **通知日志只剩十六进制、且看不到来源** — 已修复：按「文本 + 灰色十六进制」显示，并标明来源服务与特征
- **安全：WSL 解绑参数的提权命令注入** — `detach` 的 `busid` 未校验即拼进管理员 PowerShell 脚本；现加 `数字-数字` 白名单（与 `attach` 一致）
- **安全：BLE 设备名 XSS** — 详情面板未转义广播名（列表处已转义），附近设备可用构造的名称注入脚本；现统一转义
- **安全：错误上报服务鉴权** — `/report` 与查询接口此前无任何校验；现支持 `ERROR_API_KEY`（未配置时仅接受本机请求，Worker 版直接拒绝）并加每 IP 限速与字段长度上限
- **稳定性** — 锁中毒不再 panic（避免命令永不返回导致界面卡住）、BLE 通知/ADB 输出/工作流事件三处队列加上限、串口接收缓冲超限时会明确提示丢弃了多少字节

### 🔧 改进

- **读循环按可见性降频** — 串口 / WSL 监视器在所在页面隐藏或窗口最小化时，轮询由 25ms 降到 500ms，避免后台空转白耗 IPC 与 CPU；**降频不丢数据**（读线程照旧往缓冲里攒，只是一次多取）
- **蓝牙页状态随用户配置保留** — 内嵌监视器开关与宽度、已展开的服务、广播内容展开态、过滤词与展开态、选中的设备地址，重开应用后仍在（存于 `%APPDATA%\seahi-serial\config.json`）
- **写入弹窗精简** — 去掉冗余说明文字与多余的关闭图标，提示并入输入框占位符；HEX/文本、写响应/无响应、行尾三行统一为「标签 + 下拉」；输入框可拖动调整大小
- **GATT 树可读性** — 服务 / 特征名称列对齐；服务类型名称统一右对齐；展开服务若无可显示内容，按「未连接 / 只出现在广播 / 该服务下没有特征」分别提示，不再一律提示「连接设备后查看特征」
- **设备列表** — 扫描中显示「正在扫描…」；设备名旁「已连接」提示间距调整
- **报错文案本地化** — 「未找到设备」「随机地址轮换」等，便于判断原因
- **代码质量** — 若干死代码清理（`getDemoChars`、`bindMonitorEvents`、`decodeData`、`refreshWslDevices`）与监听器泄漏修复；CI 增加版本号一致性校验

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐，含 ADB platform-tools） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |
