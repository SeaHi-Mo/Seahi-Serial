## v0.2.9

### ✨ 新增

- **监控中切换端口自动重连** — 端口处于监控状态时直接选择新端口，自动释放旧端口资源并连接新端口；切换失败保持断开并提示；自动重连等待中切换端口会取消残留重试（普通串口与 WSL 串口均支持）

### 🐛 修复

- **启动界面** — 修复 JS 初始化失败时窗口变成纯黑/纯白的问题：新增启动兜底页（提示 + 重新加载/退出按钮）、8 秒初始化看门狗、Tauri 桥接缺失时的安全降级、全局错误捕获提前注册
- **白屏闪烁** — 窗口背景色改为深色，消除启动瞬间的白屏闪烁
- **WebView2 运行时** — 安装包新增缺失检测与自动安装，避免新机器上界面无法渲染
- **WSL 行尾同步** — 修复连接中修改"行尾"设置不同步到工作流引擎的问题
- **主题过渡** — 切换主题时日志区不再逐行动画卡顿
- **下拉菜单** — 空间不足时自动向上展开，端口下拉不再超出窗口被裁；监视器过多时支持横向滚动
- **设备映射配色** — 映射行背景与"WSL"状态文字跟随当前主题风格，不再固定绿色
- **引导层级** — Toast 与首次引导步骤卡层级错开，避免重叠

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |
