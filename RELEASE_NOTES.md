## v0.4.0

### ✨ 新增

- **蓝牙(BLE) 调试面板** — 扫描附近的 BLE 设备（名称/地址/地址类型/信号强度/广播内容），连接后展示完整 GATT 服务与特征树；支持读取、写入、订阅通知/指示；连接期间每 3 秒刷新信号强度
- **广播内容解析** — 展示原始广播字节并按 Bluetooth SIG AD Type 切分；解析广播名、广播服务、外观、发射功率、厂商数据（含 iBeacon/Eddystone 等）、服务数据
- **设备类型识别与图标** — 依据厂商 Company ID 与 AD type 自动识别并显示图标：标准 BLE / Apple(含 iPhone) / PC(微软) / iBeacon / Bluetooth Mesh；iBeacon 与 Mesh 在设备名后附灰色文字提示（如 `NexBeacon Pro (iBeacon)`），避免与同为 Apple 厂商的 iPhone 混淆
- **写入（发送）窗口** — 点特征的「写入」图标弹出窗口：可选**写响应 / 无响应**、**HEX / 文本**（支持 `\r \n \t` 转义），回车发送，发送后自动清空输入；成功与失败均有日志与提示

### 🐛 修复

- **重连后找不到设备** — 断开后 btleplug 会把设备移出内部表；现保留外设对象直接重连（设备停止广播时也能重连），并支持短扫描兜底
- **重连后写入报「该对象已经关闭」** — 重连会替换底层设备对象但旧 GATT 缓存未清，导致写入/订阅拿到已关闭对象；已在 vendored fork 中于 `connect()` 清理缓存（详见 `src-tauri/vendor/btleplug/VENDOR.md`）
- **切到其它页面一段时间后连接被系统回收** — WinRT 的 `GattSession.MaintainConnection` 默认为 `false`，空闲即回收链路（实测离开 150 秒必断）；fork 中置为 `true`
- **设备主动断开无感知** — 信号强度轮询顺带回报链路状态，设备侧断开后约 3 秒内自动复位为未连接并提示
- **订阅状态残留** — 断开后订阅图标仍显示「启用」；现随断开复位
- **已连接标记串台** — 换连另一台后旧设备仍显示「已连接」；现按地址清理
- **切页回来右侧详情空白** — 已连接设备若不再广播就不在扫描结果中，详情将找不到设备；现补进列表
- **安全：WSL 解绑参数的提权命令注入** — `detach` 的 `busid` 未校验即拼进管理员 PowerShell 脚本；现加 `数字-数字` 白名单（与 `attach` 一致）
- **安全：BLE 设备名 XSS** — 详情面板未转义广播名（列表处已转义），附近设备可用构造的名称注入脚本；现统一转义
- **错误上报服务鉴权** — `/report` 与查询接口此前无任何校验；现支持 `ERROR_API_KEY`（未配置时仅接受本机请求，Worker 版直接拒绝）并加每 IP 限速与字段长度上限

### 🔧 改进

- 连接/断开写入数据日志；关闭程序时主动断开设备
- 数据日志跟随设备：切换设备或断开时清空，切页保留
- 同一特征的 `write` 与 `write_without_response` 合并为一个「发送」图标（原先出现两个一模一样的图标）
- 数据日志滚动条与串口输出区风格统一；未连接设备的信号强度显示为「—」
- 「未找到设备」「随机地址轮换」等报错文案本地化，便于判断原因
- 若干死代码清理（`getDemoChars`、`bindMonitorEvents`、`decodeData`、`refreshWslDevices`）与监听器泄漏修复

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐，含 ADB platform-tools） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |

## v0.3.6

### ✨ 改进

- **ADB 工具添加至系统 PATH** — 安装时（默认勾选）将 `platform-tools` 目录加入系统 `PATH`，终端/脚本可直接使用 `adb`；卸载时精确移除该条目，不会破坏系统环境变量（安全保留 `REG_EXPAND_SZ`）

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐，含 ADB platform-tools，安装后可系统级使用 adb） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |

## v0.3.5

### ✨ 新增

- **ADB 调试** — 新增 ADB 调试面板：自动检测/列出 ADB 设备（序列号 + 状态点 + 型号），点击设备开启交互式 `adb shell` PTY 终端（xterm 渲染，支持 ANSI 颜色/光标/多列 `ls`），支持 TAB 命令补全，可一键返回主界面
- **ADB 工具免下载** — platform-tools（adb.exe）随安装包分发，运行时零下载
- **顶部工具栏图标化** — 功能按钮改为图标并统一配色随主题联动，删除应用名称文字
- **串口日志隐形缓存** — 会话级自动缓存（最多 10 个 FIFO 淘汰）

### 🐛 修复

- **ADB 会话尺寸错位/乱码** — 新增 `adb_shell_resize`，PTY 尺寸与前端容器实时同步，修复固定 40×120 布局导致的补全/提示符错位、乱码、边缘残缺
- **ADB 设备列表闪烁/占位** — 增量更新去闪烁，去掉"检测中/预留"占位；状态用颜色点表达；面板打开期间自动刷新（5s）
- **重复连接/孤儿 PTY** — 重复点击设备忽略、快速切换时回收未建成会话的 PTY

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐，含 ADB platform-tools） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |

## v0.3.2

### 🐛 修复

- **WSL 自动映射开关无法切换显示状态** — 点击开关可视滑块触发行选中逻辑，整行列表被重新渲染，干扰 checkbox 的 change 事件，使 `toggleAutoMap` 不触发/不稳定，导致开关显示状态无法切换、自动映射无法开启；修复行选中对开关（label）及其内滑块的误触发
- **串口写操作锁死** — 读线程长期持锁时，写命令的 `lock()` 会无限卡死、冻结主线程；改为带超时（`try_lock` 轮询）返回明确错误，写阻塞同时移至 `spawn_blocking`
- **WSL 命令串音** — 并发执行持久化 WSL shell 命令时，各命令读同一根管道而互相串音（A 拿到 B 的输出）；新增 `WSL_SHELL_CMD_LOCK` 串行化读写
- **WSL/串口命令阻塞 UI** — `list_ports` / `list_wsl_devices` / `check_wsl_status` / `open_wsl_serial` / `read_wsl_serial` / `send_wsl_serial` / `get_wsl_distributions` 等改为 async + `spawn_blocking`，同步长命令不再卡住主线程
- **错误上报接入** — 前后端多路径接入错误上报，便于远程诊断（前端 `reportError`、后端 `report_error`）

### ✨ 改进

- 前端 `invoke` 统一带超时兜底（`invokeTimeout`），后端命令挂起（如 UAC 未确认）时前端不再永久等待
- WSL 设备列表刷新加单飞锁，避免取消映射后多路重扫并发挤压与全量 DOM 重建
- WSL 会话改用 `Arc` 持有，慢命令不再持全局锁阻塞其它 WSL 命令

## v0.3.1

### ✨ 改进

- **WSL 设备映射授权窗口** — 首次映射需管理员权限时，弹出独立的授权确认窗口，不再依赖一闪而过的 UAC 终端；修复授权流程中"已成功却报失败 / 误报取消"等问题
- **真终端模拟器** — 终端模式重构为真正的 Linux 终端：识别设备提示符（如 `=> `、`# `、`$ `），输入光标内联跟随提示符；空回车触发换行；移除固定底部输入条；继承系统输入法；发送后不做本地回显（由设备回显）

### 🐛 修复

- 修复设备变更事件把 WSL 监视器端口误刷成 Windows COM 列表的问题
- 终端模式与日志编辑冲突：终端模式下关闭输出区 contenteditable，避免组合文本拼接/竖排/浏览器光标冲突；清空日志时保留终端当前行

## v0.3.0

### 🔒 安全加固

- **修复存储型 XSS** — 发送回显未转义直接注入 HTML，现改为 textContent，堵住远程代码执行链前端入口
- **`open_url` 命令注入** — 去掉 `cmd /C start`，改用 rundll32 + 协议白名单校验
- **更新链路收紧** — 下载 URL/SHA-256 改为后端缓存校验、安装路径限制在受控目录，杜绝任意文件下载/执行
- **日志保存限制** — `save_log` 仅允许写入最近一次选择的目录
- **错误收集服务** — 修复 `corsHeaders` 未定义、`error_hash` 缺少 UNIQUE（去重失效）、CORS 收紧到可信来源

### 🐛 修复

- **关闭串口死锁** — 读线程卡死时关闭/重连/关窗不再永久阻塞（线程 join 加超时）
- **WSL 进程泄漏** — 超时后正确终止子进程/线程，不再泄漏孤儿进程
- **list_wsl_devices 执行超时** — 缩短各步超时、无设备时不再触发提权，前端超时兜底放长，WSL 面板正常加载设备列表
- **纯 `\r` 换行** — AT 等嵌入式设备以 `\r` 换行时正确分行
- **加载历史后行号错乱** — 加载历史后重排行号
- **`addMonitor` 窗口自动加宽失效** — 修复 `size.width`→`size[0]`
- **多处竞态** — 波特率切换闭包崩溃、取消映射与断开竞争、异步调用未捕获 rejection

### ✨ 改进

- 前端轮询 10ms→25ms、读线程无数据退避，降低 IPC 与 CPU 开销
- 新增后端单元测试（parse_hex_bytes / parse_version / strip_windows_com_suffix / decode_wsl_output）

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |

## v0.2.11

### 🐛 修复

- **接收数据假换行** — 修复串口数据被轮询分块造成"莫名其妙换行"的问题：连续发送不带 `\r\n` 的文本流不再因轮询边界被拆成多行，只有设备真正发送 `\r\n` 才换行；同时改用流式 UTF-8 解码，修复多字节字符跨块时出现的乱码（U+FFFD）
- **WSL 多发行版映射** — `usbipd attach` 增加 `--distribution` 指定目标发行版，WSL 面板新增"映射到"发行版选择，修复多 WSL 系统下 UI 显示成功但设备进错发行版的问题（#19）
- **WSL 界面卡死** — 无 WSL 分发/未确认 UAC 时，usbipd 提权 `-Wait` 永久挂起导致整个程序卡死；改为轮询结果文件 + 超时，前端 invoke 也加超时保护
- **端口未释放** — 关闭/删除监视器时端口可能被持续占用；重构释放逻辑（先停读线程、try_lock 快速清理、等待带超时），前端关闭监视器时等待释放完成（#15/#16）
- **发送卡死** — 发送数据时若读线程持有端口锁会无响应；缩短锁竞争窗口，发送/快捷指令调用加超时与越界防御（#18）
- **渲染异常隔离** — 接收数据显示异常不再误触发断开/重连逻辑（#13 防御）

### ✨ 新增

- **WSL 持久化 shell 超时/杀进程重构** — 超时命令自动杀进程，避免残留进程占用端口
- **工作流条件窗口匹配** — 支持跨数据块边界的条件匹配，匹配后由后台动作线程串行执行
- **安全加固** — 收紧 CSP、防 XSS 转义、更新包 SHA256 校验
- **WSL 自动映射连续失败暂停** — 连续映射失败后自动暂停，避免反复重试

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |

## v0.2.10

### ✨ 新增

- **额外监视器复制配置** — 打开额外监视器时自动复制当前监视器的用户配置（波特率、自动滚动、行号、快捷指令、工作流规则等，深拷贝互不干扰）
- **WSL 设备状态点** — 设备列表新增连接状态圆点：绿=空闲、黄=占用/使用中、红=错误；连接/断开时即时刷新
- **WSL 设备双行信息** — 设备名下方显示 `VID:PID · SN 序列号` 副标题，便于区分同型号设备
- **WSL 设备列表行选中** — 点击整行高亮（背景 + 左侧强调条）
- **移开输出区恢复自动滚动** — 点击输出区暂停自动滚动后，鼠标移开监控区即自动恢复（并清除残留光标），无需再点击回来

### 🐛 修复

- **WSL 路径解析** — 改用 sysfs 的 `idVendor`/`idProduct` 匹配已映射设备的 WSL 路径，并正确读取 USB 产品名，修复之前解析失效导致端口列一直显示 "WSL"、下拉看不到设备名的问题
- **WSL 设备列表刷新** — 映射后自动重试解析 WSL 路径，WSL 面板打开期间每 5 秒轮询，端口列不再长时间停留在 "WSL"
- **WSL 名称/COM 显示** — 设备列表不再显示 Windows 侧名称与 COM 号，改为显示设备名（去掉 `(COMxx)` 后缀）与 WSL 路径
- **WSL 监视器下拉** — 展开时显示 `路径(设备名)`，选中后只显示路径
- **列表密度与可访问性** — 紧凑行高、列宽按内容分配、开关关闭态中性灰、控件 focus-visible 焦点环

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |

### 📦 下载

| 文件 | 说明 |
|------|------|
| `Seahi-Serial-Setup-{VERSION}.exe` | Inno Setup 安装程序（推荐） |
| `Seahi.Serial_{VERSION}_x64_en-US.msi` | MSI 安装包 |
