# Seahi Serial 测试用例

版本：v0.4.1 | 平台：Windows 10/11

> 蓝牙(BLE) 部分的**已验证/未覆盖清单与证据**见 `doc/BLE_VERIFICATION.md`（本文件只列手工用例）。
> 说明：ADB 调试面板（v0.3.5 引入）目前**尚无手工用例**，属待补项。

---

## 运行方式

测试前需以开发模式运行，确保测试的是最新代码：

```bash
# 首次运行，安装依赖
npm install

# 启动开发模式（热重载）
npm run dev
```

启动后会自动弹出应用窗口。修改代码后窗口会自动刷新，无需重新运行。

> 若需清除首次引导以便重新测试，在浏览器控制台（F12）执行：
> ```javascript
> localStorage.removeItem('onboarding_done');
> ```
> 然后刷新窗口。

---

## 一、串口基础功能

### TC-1.1 串口枚举
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 启动程序 | 串口下拉框显示「加载中...」后自动刷新出可用 COM 口列表 |
| 2 | 插入 USB 串口设备 | 设备列表自动刷新，新 COM 口出现 |
| 3 | 拔出 USB 串口设备 | 设备列表自动刷新，对应 COM 口消失 |
| 4 | 插入多个串口设备 | 所有设备均正确显示，friendly name 与 product name 正确解析 |

### TC-1.2 串口连接/断开
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 选择一个串口，点击「开始监控」 | 按钮变为「停止监控」（主题色背景），串口成功打开 |
| 2 | 连接状态下点击「停止监控」 | 按钮恢复「开始监控」（低调底色），串口关闭 |
| 3 | 未选择任何串口时点击「开始监控」 | 提示错误或无响应 |
| 4 | 连接已占用的串口 | 提示错误信息 |
| 5 | 连接后拔出设备 | 自动断开（若开启自动重连则尝试重连） |

### TC-1.3 波特率设置
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 修改波特率输入框为 9600，点击「开始监控」 | 以 9600 波特率连接 |
| 2 | 点击波特率下拉箭头，选择 115200 | 输入框更新为 115200 |
| 3 | 输入非法值（如 0 或负数） | 连接时提示错误 |
| 4 | 输入框直接输入 4000000 | 最大支持 4000000 |

### TC-1.4 数据位/停止位/校验位
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 展开高级设置，修改数据位为 7 | 连接后以 7 数据位通信 |
| 2 | 修改停止位为 2 | 连接后以 2 停止位通信 |
| 3 | 修改校验位为偶校验 | 连接后以偶校验通信 |

### TC-1.5 DTR/RTS 信号
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 连接串口后，展开高级设置，勾选 DTR | DTR 信号拉高 |
| 2 | 取消勾选 DTR | DTR 信号拉低 |
| 3 | 勾选/取消勾选 RTS | RTS 信号相应变化 |

### TC-1.6 数据接收
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 连接串口，外部设备发送 ASCII 文本 | 输出区域显示文本内容 |
| 2 | 切换查看模式为 HEX | 输出区域显示十六进制格式 |
| 3 | 切换回文本模式 | 输出区域恢复文本显示 |
| 4 | 开启「自动滚动」 | 新数据到来时自动滚动到底部 |
| 5 | 关闭「自动滚动」 | 新数据到来时不滚动 |
| 6 | 开启「时间戳」 | 每条数据前显示时间戳 |
| 7 | 开启「行号」 | 每行数据前显示行号 |

### TC-1.7 数据发送
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在输入框输入文本，按回车 | 数据成功发送 |
| 2 | 点击「发送」按钮 | 数据成功发送 |
| 3 | 切换发送模式为 HEX，输入 `48 65 6C 6C 6F` | 发送 ASCII "Hello" |
| 4 | 切换行尾为 CRLF，发送 "test" | 发送 "test\r\n" |
| 5 | 切换行尾为 LF，发送 "test" | 发送 "test\n" |
| 6 | 切换行尾为 CR，发送 "test" | 发送 "test\r" |
| 7 | 切换行尾为无，发送 "test" | 发送 "test" |

### TC-1.8 发送历史
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 发送 "hello"，然后清空输入框，按方向键上 | 输入框显示 "hello" |
| 2 | 连续发送多条不同内容 | 方向键上下可遍历所有历史 |
| 3 | 按 Escape | 发送历史下拉关闭 |

### TC-1.9 终端模式
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 开启终端模式 | 输出区域变为可编辑状态 |
| 2 | 在输出区域输入字符 | 字符发送到串口 |
| 3 | 按回车 | 发送回车 |
| 4 | 按退格键 | 发送退格 |
| 5 | 关闭终端模式 | 输出区域恢复只读 |

---

## 二、快速指令（监控区最右侧的可折叠分栏）

### TC-2.1 快速指令分栏的展开与收起
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 打开监视器（不做任何操作） | 输出区右侧只有一条 14px 指示条（**无图标无文字**），分栏**默认折叠**；指示条上下与输出区齐平，不越过工具栏与发送栏 |
| 2 | 点击该折叠条 | 分栏展开（240px），**整条折叠条填成主题蓝**（悬停再亮一档），握把在蓝底上转成白色，提示变为「收起快速指令（左右拖动可调宽）」 |
| 3 | 再点一次折叠条 | 分栏收起，蓝色填充消失、回到蓝色细握把，提示恢复「展开快速指令」 |
| 4 | 展开后按住折叠条往左拖 | 分栏变宽（往左拖 = 变宽），拖动过程中不加过渡、跟手；松手后宽度保留 |
| 5 | 一直往左拖 / 往右拖到极限 | 宽度钳在 120–640px，且至少给输出区留 160px |
| 6 | 折叠态按住折叠条拖动 | **不进入拖动**（折叠态只有"点击展开"，光标也是 pointer 而非 col-resize） |
| 7 | 展开状态下观察工具栏与发送栏 | 两栏仍占满窗格宽度，只有输出区被分栏挤窄 |
| 8 | 调宽后收起再展开、或重启应用 | 沿用调过的宽度（写入 config.json） |
| 9 | 关闭并重开监视器 | 分栏回到默认折叠态（展开态不持久化），指令内容仍在 |

### TC-2.2 快速指令编辑与发送
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 添加指令，设置标签为 "AT"，值为 "AT\r\n" | 标签和值保存成功 |
| 2 | 点击指令的发送按钮 | 对应内容通过串口发送 |
| 3 | 修改已有指令的标签或值 | 修改自动保存 |
| 4 | 点击指令的删除按钮 | 指令被删除 |
| 5 | 未连接串口时 | 每条指令的发送按钮置灰不可点 |

### TC-2.3 外部文件（导入 / 导出 / 写回：文件即存储）
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点标题行右侧「导入」，选一个 Markdown 表格（或 TSV / 纯指令行）文件 | 列表变成文件内容；来源行出现「文件名 + 重载 + 断开」 |
| 2 | 点「＋ 添加」 | 新条目**写进文件**（去抖约 0.6s；用编辑器打开文件确认那一行在） |
| 3 | 改名称/内容、删掉一条 | 同样写回文件（对应行更新 / 消失） |
| 4 | 检查文件里原有的 `#` 注释、表头、`\|---\|` 分隔行、第 3 列起的备注 | 写回后仍在原位 —— 不会被重排或抹掉；原本只写一列的行也不会被补成两列 |
| 5 | 用编辑器在文件里加一行，再点「重载」 | 列表出现新增那行（以文件为准） |
| 6 | 挂着文件时用编辑器改文件，然后在面板里改一条 | 提示「冲突：文件已被其它程序修改…」；界面列表不动（**不覆盖**外部改动） |
| 7 | 点「断开」 | 不再写回文件；此后增删只进 config.json |
| 8 | 点「导出」 | 弹保存框另存一份副本（Markdown / TSV），**不改变**当前挂载目标 |
| 9 | 选一个含逗号的指令文件（如 `AT+CWJAP="a","b"`） | 该行完整读成**一条**指令，不会被逗号切成多条 |
| 10 | 选一个 GBK 编码的文件 | 正常读入；写回后仍是 GBK（不会变成乱码或悄悄转 UTF-8） |
| 11 | 选超限文件（>256 KB / >500 条 / 超长行） | 明确提示（超限行跳过并给行号与原因），不是整份失败、也不是静默截断 |
| 12 | 让两个监视器导入同一个文件 | 第二个被拒绝，并提示先去前一个「断开」（避免互相写回） |
| 13 | 文件开头带 YAML 文件头（`---` … `---`，或 TOML 的 `+++`） | 文件头**原样保留**、不会变成指令，写回后一字不动；若含 `baud`/`mode`/`lineEnding`/`delay`/`expect`/`timeout`/`hex` 这类 key，会明确提示「暂不生效（发送沿用面板设置）」 |
| 14 | 文件头缺收尾行（只写了一行 `---`） | 只把第一行当头部并提示，正文照常解析（不会整份被吞） |

---

## 三、日志功能

### TC-3.1 日志目录与保存
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 展开高级设置，点击「选择日志目录」 | 弹出文件夹选择对话框 |
| 2 | 选择目录后 | 目录路径显示在界面上 |
| 3 | 连接串口并接收数据后，点击保存日志 | 日志文件保存到指定目录 |
| 4 | 复制全部输出 | 输出内容复制到剪贴板 |

### TC-3.2 清空输出
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 接收若干数据后点击清空按钮 | 输出区域清空，行号重置 |

---

## 四、多监视器

### TC-4.1 打开/关闭额外监视器
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击全局栏「＋ 打开额外监视器」 | 新增一个监视器面板，分栏显示 |
| 2 | 两个面板各自连接不同串口 | 互不干扰，独立收发 |
| 3 | 点击额外监视器的关闭按钮 | 该监视器关闭，只剩主监视器 |

---

## 五、WSL 端口映射

### TC-5.1 打开/关闭 WSL 面板
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击「WSL 端口映射」 | 切换到 WSL 映射面板，主监视器隐藏 |
| 2 | 点击「返回监视器」 | 切换回主监视器，WSL 面板隐藏 |

### TC-5.2 WSL 发行版管理
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 打开 WSL 面板 | 显示所有 WSL 发行版卡片（名称、状态、运行时间、内存） |
| 2 | 点击已停止发行版的「启动」 | 按钮变为「关闭」（主题色），状态灯变绿 |
| 3 | 点击运行中发行版的「关闭」 | 发行版关闭，状态灯变灰 |
| 4 | 运行中的发行版 | UP 时间每秒递增，MEM 数据实时更新 |

### TC-5.3 USB 设备映射
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 打开 WSL 面板，查看设备列表 | 显示 BUSID、端口、设备名称、自动/映射列 |
| 2 | 勾选设备的映射复选框 | 显示加载中，完成后设备行变为已映射样式（绿色边框） |
| 3 | 取消勾选已映射的设备 | 设备解除映射，恢复默认样式 |
| 4 | 开启设备的「自动」开关 | 设备拔插后自动重新映射 |
| 5 | 关闭「自动」开关 | 设备拔插后不再自动映射 |

### TC-5.4 WSL 串口监控
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 映射设备后，WSL 串口下拉框刷新 | 显示 WSL 内的 /dev/ttyACM*、/dev/ttyUSB* 等设备 |
| 2 | 选择 WSL 串口，点击「开始监控」 | 通过 bridge 连接 WSL 串口 |
| 3 | 在 WSL 串口中收发数据 | 数据正常传输 |
| 4 | 点击「停止监控」 | WSL 串口断开 |

### TC-5.5 分隔线拖拽
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 拖拽设备列表与监控面板之间的分隔线 | 两个面板高度实时调整 |
| 2 | 松开鼠标 | 面板高度固定，保存到配置 |
| 3 | 缩放窗口 | 两个面板按比例自适应 |
| 4 | 拖拽到接近底部 | 监控面板不溢出窗口，发送框始终可见 |

---

## 六、主题系统

### TC-6.1 深浅色切换
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击右上角主题切换开关 | 在深色和浅色模式间切换 |
| 2 | 切换后观察所有 UI 元素 | 颜色正确变化，无残影或错位 |
| 3 | 跟随系统主题时 | 首次启动自动匹配系统深浅色 |

### TC-6.2 主题风格切换
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击全局栏的主题风格下拉 | 显示 6 种风格：默认、浮世绘彩、诗意东方、水墨丹青、桃之夭夭、金风玉露 |
| 2 | 切换为「浮世绘彩」 | 整体配色变为浮世绘风格 |
| 3 | 在浮世绘风格下切换深浅色 | 深色/浅色配色均正确 |
| 4 | 逐一切换所有风格 | 每种风格的深色和浅色均无异常 |

---

## 七、窗口行为

### TC-7.1 窗口缩放
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 拖拽窗口边缘缩小 | 内容自适应缩放，不出现滚动条或截断 |
| 2 | 拖拽窗口边缘放大 | 内容自适应扩展，布局不变形 |
| 3 | 最小化后恢复 | 界面正常显示 |
| 4 | 最大化/还原 | 布局正确适应 |

### TC-7.2 分栏布局
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 单监视器状态 | 监视器占满整个区域 |
| 2 | 打开额外监视器 | 两个面板等宽分栏，中间有蓝色分隔线 |
| 3 | 关闭额外监视器 | 恢复单面板全屏 |

### TC-7.3 窗口位置与大小记忆（v0.5.1 起由 Rust 端 `window.json` 管）
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 启动应用 | 窗口出现在上次的位置与大小，**不是**先闪一下默认几何再跳过去（主窗口是 `visible:false`，恢复完几何才显示） |
| 2 | 把窗口拖到副屏、改成一个不等边尺寸，关闭 | `%APPDATA%\seahi-serial\window.json` 里的 `x`/`y`/`width`/`height` 是退出前的值 |
| 3 | 重新打开 | 位置与大小与上次完全一致（反复开关 3 次，位置不往右下漂移） |
| 4 | 最大化后关闭再打开 | 起来仍是最大化，且**取消最大化后**回到最大化之前的那个普通尺寸（不是全屏尺寸） |
| 5 | 最小化后关闭再打开 | 普通几何不被 `-32000` 之类的哨兵坐标污染，仍回到最小化前的尺寸 |
| 6 | 删除 `window.json` 后打开（模拟老版本升级） | 尺寸回退用 `config.json` 里的 `windowWidth`/`windowHeight`，**位置保持居中**（不跳到左上角） |
| 7 | 把窗口移到副屏后拔掉该显示器，再打开 | 窗口回到可见显示器内（`rect_on_screen` 判定：至少露出 60×40），不会落在看不见的地方 |
| 8 | 临时把 `src/index.html` 的初始化打断（如开头 `throw new Error('x')`） | 窗口**仍然显示**并给出兜底错误页（`revealMainWindow` 与 Rust 4 秒兜底各一道），不会出现"进程在跑但没有窗口" |

---

## 八、配置持久化

### TC-8.1 配置保存/加载
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 设置波特率为 9600，切换主题风格为浮世绘彩 | 配置自动保存 |
| 2 | 关闭程序并重新打开 | 波特率仍为 9600，主题为浮世绘彩 |
| 3 | 添加快捷指令后关闭重开 | 快捷指令保留 |
| 4 | WSL 面板调整分隔线后关闭重开 | 分隔线位置保留 |

### TC-8.2 首次使用引导
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 清除 localStorage 中的 `onboarding_done`，刷新页面 | 引导自动弹出 |
| 2 | 引导共 9 步，每步聚光灯高亮对应元素 | 聚光灯准确、卡片不溢出视口 |
| 3 | 点击「下一步」逐步浏览 | 步骤指示点同步更新 |
| 4 | 点击「跳过」或点击外部区域 | 引导关闭，不再自动弹出 |
| 5 | 最后一步点击「开始使用」 | 引导关闭 |

---

## 九、自动更新

### TC-9.1 更新检测
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 启动程序 | 自动检测 GitHub 最新 Release |
| 2 | 有新版本时 | 全局栏显示「更新」按钮 |
| 3 | 点击「更新」 | 下载并安装更新 |
| 4 | 无新版本时 | 不显示更新按钮 |

---

## 十、异常场景

### TC-10.1 设备热插拔
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 连接串口后拔出设备 | 自动检测断开，显示错误提示 |
| 2 | 开启自动重连后拔出再插入 | 自动恢复连接 |
| 3 | WSL 映射的设备拔出 | 自动解除映射，设备列表更新 |

### TC-10.2 错误处理
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 尝试打开不存在的串口 | 显示错误 toast |
| 2 | 发送数据时串口已断开 | 显示错误 toast |
| 3 | WSL 未安装时打开映射面板 | 提示 WSL 未安装 |
| 4 | usbipd 未安装时尝试映射 | 提示安装 usbipd-win |

### TC-10.3 提交 Issue
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击全局栏「提交 issue」 | 打开浏览器跳转到 GitHub Issues 页面 |

---

## 十一、蓝牙(BLE) 调试

> 前置：电脑蓝牙已开启；附近有可连接的 BLE 设备。详细验证记录见 `doc/BLE_VERIFICATION.md`。

### TC-11.1 扫描与列表
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击顶部「蓝牙调试」 | 进入蓝牙面板，右侧提示「从左侧选择设备…」 |
| 2 | 点击「开始扫描」 | 列表出现设备（名称/地址/地址类型/RSSI/信号条） |
| 3 | 观察图标 | 按类型显示：蓝牙 / Apple / 微软 / iBeacon / Mesh |
| 4 | 观察 iBeacon、Mesh 设备 | 设备名后出现灰色 `(iBeacon)` / `(Mesh)` 提示 |
| 5 | 点击任一设备 | 右侧出现广播内容与 GATT 服务区；展开「广播内容」可见原始字节与按 AD type 切分的段 |

### TC-11.2 连接与状态
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击「连接设备」 | 按钮变为「断开设备」，数据日志出现 `[连接中]` / `[连接成功] … 服务 N · 特征 M` |
| 2 | 观察设备列表 | 该设备出现绿色「已连接」提示，且**只有它一台**有该提示 |
| 3 | 等待 3~6 秒 | 详情头部的 RSSI 数值周期刷新 |
| 4 | 切到串口页停留 **≥150 秒**再切回 | 仍为「断开设备」（连接未被系统回收），日志与 GATT 树保留 |
| 5 | 点击「断开设备」 | 按钮恢复「连接设备」，数据日志清空，「已连接」提示消失 |

### TC-11.3 GATT 与读写
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 展开自定义服务（如 `0x…1912`） | 特征行右侧按属性显示动作图标（读取/写入/通知） |
| 2 | 找**同时支持 write 与 write_without_response** 的特征 | **只出现一个**写入图标（不应有两个相同图标） |
| 3 | 点击「读取」图标 | 日志出现 `[读取] <uuid>: <hex>` |
| 4 | 点击「写入」图标 | 弹出「写入特征值」窗口，显示目标特征，输入框自动聚焦 |
| 5 | 输入 `0102` 后回车（文本模式） | 提示「发送成功：0x30313032」，日志有 `[发送] …（文本 · 写响应）` 与 `[发送成功] … 4 字节`，**输入框被清空** |
| 6 | 切到 HEX 模式输入 `01 02` | 发送内容为 `0x0102`（与文本模式不同） |
| 7 | 切换「写响应 / 无响应」 | 日志中的写入方式标记随之变化 |
| 8 | 点击「通知」图标 | 图标变为已启用；设备上行数据时日志出现 `[通知]` |
| 9 | 断开连接 | 通知/订阅图标复位为未启用 |

### TC-11.4 重连与异常
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 断开后**立即**重连同一设备并发送数据 | 连接与发送均成功（**不应**出现「该对象已经关闭」） |
| 2 | 连接一台随后停止广播的设备 | 该设备仍在列表中并标注「已连接」，右侧详情不空白 |
| 3 | 保持蓝牙面板打开，把设备关机或移出范围 | 约 3 秒内自动变为「连接设备」，提示「设备已断开连接」，日志留一行 `[已断开] 设备侧断开或超出范围（地址）` |
| 4 | 在其它页面把设备关机后再切回蓝牙页 | 显示为未连接，日志留同样一行原因 |
| 5 | 关闭程序 | 设备侧连接被断开（`%TEMP%\seahi-serial-debug.log` 有 `CloseRequested: BLE disconnected`） |
