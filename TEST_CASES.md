# Seahi Serial 测试用例

版本：v0.4.1 | 平台：Windows 10/11

> 蓝牙(BLE) 部分的**已验证/未覆盖清单与证据**见 `doc/BLE_VERIFICATION.md`（本文件只列手工用例）。
> 说明：ADB 调试面板（v0.3.5 引入）目前**尚无手工用例**，属待补项。

---

## 运行方式

测试前需以开发模式运行，确保测试的是最新代码：

```bash
# 首次运行，安装依赖
npm install

# 启动开发模式（热重载）
npm run dev
```

启动后会自动弹出应用窗口。修改代码后窗口会自动刷新，无需重新运行。

> 若需清除首次引导以便重新测试，在浏览器控制台（F12）执行：
> ```javascript
> localStorage.removeItem('onboarding_done');
> ```
> 然后刷新窗口。

---

## 一、串口基础功能

### TC-1.1 串口枚举
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 启动程序 | 串口下拉框显示「加载中...」后自动刷新出可用 COM 口列表 |
| 2 | 插入 USB 串口设备 | 设备列表自动刷新，新 COM 口出现 |
| 3 | 拔出 USB 串口设备 | 设备列表自动刷新，对应 COM 口消失 |
| 4 | 插入多个串口设备 | 所有设备均正确显示，friendly name 与 product name 正确解析 |

### TC-1.2 串口连接/断开
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 选择一个串口，点击「开始监控」 | 按钮变为「停止监控」（主题色背景），串口成功打开 |
| 2 | 连接状态下点击「停止监控」 | 按钮恢复「开始监控」（低调底色），串口关闭 |
| 3 | 未选择任何串口时点击「开始监控」 | 提示错误或无响应 |
| 4 | 连接已占用的串口 | 提示错误信息 |
| 5 | 连接后拔出设备 | 自动断开（若开启自动重连则尝试重连） |

### TC-1.3 波特率设置
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 修改波特率输入框为 9600，点击「开始监控」 | 以 9600 波特率连接 |
| 2 | 点击波特率下拉箭头，选择 115200 | 输入框更新为 115200 |
| 3 | 输入非法值（如 0 或负数） | 连接时提示错误 |
| 4 | 输入框直接输入 4000000 | 最大支持 4000000 |

### TC-1.4 数据位/停止位/校验位
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 展开高级设置，修改数据位为 7 | 连接后以 7 数据位通信 |
| 2 | 修改停止位为 2 | 连接后以 2 停止位通信 |
| 3 | 修改校验位为偶校验 | 连接后以偶校验通信 |

### TC-1.5 DTR/RTS 信号
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 连接串口后，展开高级设置，勾选 DTR | DTR 信号拉高 |
| 2 | 取消勾选 DTR | DTR 信号拉低 |
| 3 | 勾选/取消勾选 RTS | RTS 信号相应变化 |

### TC-1.6 数据接收
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 连接串口，外部设备发送 ASCII 文本 | 输出区域显示文本内容 |
| 2 | 切换查看模式为 HEX | 输出区域显示十六进制格式 |
| 3 | 切换回文本模式 | 输出区域恢复文本显示 |
| 4 | 开启「自动滚动」 | 新数据到来时自动滚动到底部 |
| 5 | 关闭「自动滚动」 | 新数据到来时不滚动 |
| 6 | 开启「时间戳」 | 每条数据前显示时间戳 |
| 7 | 开启「行号」 | 每行数据前显示行号 |

### TC-1.7 数据发送
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在输入框输入文本，按回车 | 数据成功发送 |
| 2 | 点击「发送」按钮 | 数据成功发送 |
| 3 | 切换发送模式为 HEX，输入 `48 65 6C 6C 6F` | 发送 ASCII "Hello" |
| 4 | 切换行尾为 CRLF，发送 "test" | 发送 "test\r\n" |
| 5 | 切换行尾为 LF，发送 "test" | 发送 "test\n" |
| 6 | 切换行尾为 CR，发送 "test" | 发送 "test\r" |
| 7 | 切换行尾为无，发送 "test" | 发送 "test" |

### TC-1.8 发送历史
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 发送 "hello"，然后清空输入框，按方向键上 | 输入框显示 "hello" |
| 2 | 连续发送多条不同内容 | 方向键上下可遍历所有历史 |
| 3 | 按 Escape | 发送历史下拉关闭 |

### TC-1.9 终端模式
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 开启终端模式 | 输出区域变为可编辑状态 |
| 2 | 在输出区域输入字符 | 字符发送到串口 |
| 3 | 按回车 | 发送回车 |
| 4 | 按退格键 | 发送退格 |
| 5 | 关闭终端模式 | 输出区域恢复只读 |

---

## 二、快速指令（监控区最右侧的可折叠分栏）

### TC-2.1 快速指令分栏的展开与收起
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 打开监视器（不做任何操作） | 输出区右侧只有一条 14px 指示条（**无图标无文字**），分栏**默认折叠**；指示条中间一根细握把；指示条上下与输出区齐平，不越过工具栏与发送栏 |
| 2 | 点击该折叠条 | 分栏展开（**300px**，一条六格要放得下）：握把长成**顶到面板左缘、贯穿整栏的 6px 竖色条**（悬停再亮一档），同时面板那条 1px 分栏线退位 —— 全栏只有**一条**竖线；提示变为「收起快速指令（左右拖动可调宽）」 |
| 3 | 再点一次折叠条 | 分栏收起，竖直色条缩回**居中的细握把**（3×34，主题色 45% 透明度），提示恢复「展开快速指令」 |
| 4 | 展开后按住折叠条往左拖 | 分栏变宽（往左拖 = 变宽），拖动过程中不加过渡、跟手；松手后宽度保留 |
| 5 | 一直往左拖 / 往右拖到极限 | 宽度下限钳在 120px；上限**跟着窗格走**：`min(窗格 × 60%, 窗格 − 160px)`（窗口拉大就能拖得更宽，不再是写死的 640px；窗口变小也不会把输出区挤没） |
| 6 | 折叠态按住折叠条拖动 | **不进入拖动**（折叠态只有"点击展开"，光标也是 pointer 而非 col-resize） |
| 7 | 展开状态下观察工具栏与发送栏 | 两栏仍占满窗格宽度，只有输出区被分栏挤窄 |
| 8 | 调宽后收起再展开、或重启应用 | 沿用调过的宽度（写入 config.json） |
| 9 | 关闭并重开监视器 | 分栏回到默认折叠态（展开态不持久化），指令内容仍在 |

### TC-2.2 快速指令编辑与发送
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 展开分栏，看标题行 | **分两端**：左边是 `循环发送` 开关（状态），右边是 `＋新建循环组 / 导入 / 导出`（动作）；**没有「快速指令」文字标题**，工具栏里也**没有**「＋添加」（它在每组抬头里） |
| 1b | 窗口拉宽后看标题行 | `＋新建循环组 / 导入 / 导出` 仍**贴在面板右侧**，循环发送开关在左侧（两端分开，不会跟着窗口变宽而错位） |
| 2 | 看每一组 | 每组是**一张自己的表**：`组抬头 → 列标题 → 数据行`。抬头 = `拖动握把（最左） · 折叠 · 组名（点一下可改） · N 条 · ＋添加 · 删组`；列标题 = `顺序 │ 指令 │ 延时(ms) │ HEX`（与数据列**逐列对齐**，`(ms)` 是小一号的次要字）。行数多到出滚动条时，**列标题那条线要与每行下方的线一样长**（都止于滚动条左侧） |
| 3 | 看每一条指令 | 一行六格：**方形顺序号框**（默认 0）· 指令内容 · 延时框（默认 1000）· `HEX` 按钮（默认关）· 发送 · 删除；**没有名称/标签输入框**（原来的「指令1」「指令2」占位名已删除） |
| 4 | 填内容后点发送按钮 | 对应内容通过串口发送；按本条自己的 HEX 开关决定文本/HEX（不受主发送栏影响） |
| 5 | 在内容框里按 Enter | 同发送按钮 |
| 6 | 点某条的 `HEX` 按钮 | 该条点亮，本条按 HEX 格式发送（`01 02 03` → 3 个字节）；再点一下关闭 |
| 7 | 修改顺序号 | 只收数字、去掉多余前导零；填 >0 时方框点亮（= 参与循环发送）；清空/填 0 即退出循环列表 |
| 8 | 修改延时框并移开焦点 | 生效（毫秒）；清空自动回到 1000；填超过 600000 夹到 10 分钟 |
| 9 | 点某个组抬头里的「＋ 添加」 | 在**那一组末尾**新增一条空指令，顺序号 0 / 延时 1000 / HEX 关；名称栏不再出现 |
| 10 | 点某条的删除按钮 | 该条被删除 |
| 11 | 未连接串口时 | 每条指令的发送按钮置灰不可点 |

### TC-2.3 循环发送
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 没连接串口时点「循环发送」 | **拒绝开启**并提示「还没打开监控…」，开关保持关闭态 |
| 2 | 全部顺序号都是 0 时点「循环发送」 | **拒绝开启**并提示「没有顺序号大于 0 的指令…」，开关保持关闭态 |
| 3 | 把三条指令的顺序号设为 1 / 2 / 3（顺序号 0 的留一条） | 三个方框点亮；点「循环发送」→ 开关填主题色，立刻发**最上面那一组里**顺序号 1 的那条 |
| 4 | 观察发送顺序 | **组的上下顺序 → 组内顺序号**：先把第一组按 1 → 2 → 3 发完，再进第二组，走完最后一组回到最上面那组；每发完一条等**它自己的延时**；顺序号 0 的那条始终不参与 |
| 5 | 等一轮发完 | 从顺序号最小的那条重新开始（是循环，不是只跑一遍） |
| 6 | 跑着的时候改某条的顺序号 / 延时 / 删掉一条 | 下一步立刻按新设置走，不需要重启循环 |
| 7 | 跑着的时候再点一次「循环发送」 | 立即停止（定时器被清掉，不会再冒出下一发），开关恢复常态 |
| 8 | 跑着的时候拔掉串口 / 点「停止监控」 | 循环**立刻自愈停止**并提示「监控已断开」，开关回到关闭态（不留还在倒计时的定时器） |
| 9 | 跑着的时候把最后一条的顺序号也改回 0 | 下一拍自愈停止并提示「已经没有顺序号大于 0 的指令」 |
| 10 | 循环进行中把分栏收起（点折叠条） | 循环**不停**（收起只是看不看得到的问题），但折叠条底部点着一颗一闪一闪的小点、`title` 写着「循环发送进行中，展开可停止」—— 不能出现"界面看不出在发数据、设备却一直在收"的状态 |
| 11 | 跑着的时候关掉这个监视器分栏 | 定时器一起收掉（不残留） |
| 4b | 点「＋ 新建循环组」 | **追加到最下面**一组，默认带 1 条空指令；组名默认「循环 N」，点名字可改 |
| 4c | 按住某组抬头左侧的握把上下拖 | 拖过邻组中线即换位；**组的上下顺序就是循环顺序** —— 拖到最上面的那组就是下一次循环的起点；松手后文件里那几张表也跟着换序 |
| 4d | 点组抬头里的折叠箭头 | 只收起这一组的列标题与数据行（抬头还在，进度可见）；组名与折叠状态随 `config.json` 保留 |
| 4e | 试着删掉最后一组 | **拒绝**并提示「至少要留一组」 || 12 | 重启应用 | 循环发送是**关**的（开关状态不持久化 —— 开机自动发指令太危险），顺序号/延时/HEX 保留 |

### TC-2.4 外部文件（导入 / 导出 / 写回：文件即存储）
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点标题行右侧「导入」，选一个 Markdown 表格（或 TSV / 纯指令行）文件 | 列表变成文件内容；来源行出现「文件名 + 重载 + 断开」。文件里**每个 `## 组名` 抬头 + 它下面那张表 = 一个循环组**（老文件没有抬头 → 只有一组） |
| 2 | 点「＋ 添加」 | 新条目**暂时不写进文件**（它还没有内容，写一行空行进去也活不过一次重载）；**填上内容后立即写回**（去抖约 0.6s） |
| 3 | 改内容、删掉一条 | 同样写回文件（对应行更新 / 消失） |
| 4 | 检查文件里原有的 `#` 注释、表头、`\|---\|` 分隔行、第 3 列起的备注 | 写回后仍在原位 —— 不会被重排或抹掉；原本只写一列的行也不会被补成两列 |
| 5 | 给列表设好顺序号 / 延时 / HEX，再看文件 | 若**表头声明了**这三列（`\| 顺序号 \| 延时(ms) \| HEX \|`），值就写在那几列里；**表头没写这三列**则写回一字不多（用户的表结构由用户定），此时这三项只保存在本机配置里 —— 导入这种文件时会**提示一次**「点导出可得到带三列的文件」 |
| 6 | 手工在表头加上 `顺序号 / 延时(ms) / HEX` 三列并填值，重新「导入」 | 三列被认出来：方框里出现顺序号、延时与 HEX 也照文件走；文件里没填的格子保持空（不会被硬写成 0 / 1000） |
| 7 | 把第 3 列写成「备注甲」（表头写 `备注`，不是 `顺序号`） | 那一列**原样保留**（绝不被当成顺序号、也不会被改写成 `0`）；`编号`/`no` 这类列名同样不认 |
| 8 | 改一条指令的顺序号/延时/HEX 后看文件 | 只有对应那几格变；没碰过的格子保持你写的字（写 `0`、`是`、留空都原样） |
| 9 | 给几条指令设好顺序号/延时/HEX，再点「重载」 | 表里写着的以文件为准；文件没声明那几列时按**指令内容**带回来（内容没改就保住），改动过的那条回到默认 0 / 1000 / 关 |
| 10 | 用编辑器在文件里加一行，再点「重载」 | 列表出现新增那行（以文件为准） |
| 11 | 挂着文件时用编辑器改文件，然后在面板里改一条 | 提示「冲突：文件已被其它程序修改…」；界面列表不动（**不覆盖**外部改动） |
| 12 | 点「断开」 | 不再写回文件；此后增删只进 config.json |
| 13 | 点「导出」 | 弹保存框另存一份副本（Markdown / TSV），**不改变**当前挂载目标。副本里**列序与面板一致**：`顺序号 │ 指令 │ 延时(ms) │ HEX`（缺省也写全 `0 / 1000 / false`；HEX 列写布尔字面 `true` / `false`），**不带「名称」列**（面板没有名称入口），且**面板上有几条就写几行**（空条目也占一行）；纯指令行文件放不下时会升级成 Markdown 表格并提示 |
| 14 | 把刚导出的那份文件再「导入」一次 | 指令、顺序号、延时、HEX 全部原样回来，**条数也与面板一致**（空条目那几行照样算一条） |
| 15 | 选一个含逗号的指令文件（如 `AT+CWJAP="a","b"`） | 该行完整读成**一条**指令，不会被逗号切成多条 |
| 16 | 选一个 GBK 编码的文件 | 正常读入；写回后仍是 GBK（不会变成乱码或悄悄转 UTF-8） |
| 17 | 选超限文件（>256 KB / >500 条 / 超长行） | 明确提示（超限行跳过并给行号与原因），不是整份失败、也不是静默截断 |
| 18 | 让两个监视器导入同一个文件 | 第二个被拒绝，并提示先去前一个「断开」（避免互相写回） |
| 19 | 文件开头带 YAML 文件头（`---` … `---`，或 TOML 的 `+++`） | 文件头**原样保留**、不会变成指令，写回后一字不动；若含 `baud`/`mode`/`lineEnding`/`delay`/`expect`/`timeout`/`hex` 这类 key，会明确提示「暂不生效（发送沿用面板设置；延时/HEX 是每条自己的设置，写在表头声明的列里）」 |
| 20 | 文件头缺收尾行（只写了一行 `---`） | 只把第一行当头部并提示，正文照常解析（不会整份被吞） |

---

## 三、日志功能

### TC-3.1 日志目录与保存
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 展开高级设置，点击「选择日志目录」 | 弹出文件夹选择对话框 |
| 2 | 选择目录后 | 目录路径显示在界面上 |
| 3 | 连接串口并接收数据后，点击保存日志 | 日志文件保存到指定目录 |
| 4 | 复制全部输出 | 输出内容复制到剪贴板 |

### TC-3.2 清空输出
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 接收若干数据后点击清空按钮 | 输出区域清空，行号重置 |

---

## 四、多监视器

### TC-4.1 打开/关闭额外监视器
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击全局栏「＋ 打开额外监视器」 | 新增一个监视器面板，分栏显示 |
| 2 | 两个面板各自连接不同串口 | 互不干扰，独立收发 |
| 3 | 点击额外监视器的关闭按钮 | 该监视器关闭，只剩主监视器 |

---

## 五、WSL 端口映射

### TC-5.1 打开/关闭 WSL 面板
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击「WSL 端口映射」 | 切换到 WSL 映射面板，主监视器隐藏 |
| 2 | 点击「返回监视器」 | 切换回主监视器，WSL 面板隐藏 |

### TC-5.2 WSL 发行版管理
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 打开 WSL 面板 | 显示所有 WSL 发行版卡片（名称、状态、运行时间、内存） |
| 2 | 点击已停止发行版的「启动」 | 按钮变为「关闭」（主题色），状态灯变绿 |
| 3 | 点击运行中发行版的「关闭」 | 发行版关闭，状态灯变灰 |
| 4 | 运行中的发行版 | UP 时间每秒递增，MEM 数据实时更新 |

### TC-5.3 USB 设备映射
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 打开 WSL 面板，查看设备列表 | 显示 BUSID、端口、设备名称、自动/映射列 |
| 2 | 勾选设备的映射复选框 | 显示加载中，完成后设备行变为已映射样式（绿色边框） |
| 3 | 取消勾选已映射的设备 | 设备解除映射，恢复默认样式 |
| 4 | 开启设备的「自动」开关 | 设备拔插后自动重新映射 |
| 5 | 关闭「自动」开关 | 设备拔插后不再自动映射 |

### TC-5.4 WSL 串口监控
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 映射设备后，WSL 串口下拉框刷新 | 显示 WSL 内的 /dev/ttyACM*、/dev/ttyUSB* 等设备 |
| 2 | 选择 WSL 串口，点击「开始监控」 | 通过 bridge 连接 WSL 串口 |
| 3 | 在 WSL 串口中收发数据 | 数据正常传输 |
| 4 | 点击「停止监控」 | WSL 串口断开 |

### TC-5.5 分隔线拖拽
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 拖拽设备列表与监控面板之间的分隔线 | 两个面板高度实时调整 |
| 2 | 松开鼠标 | 面板高度固定，保存到配置 |
| 3 | 缩放窗口 | 两个面板按比例自适应 |
| 4 | 拖拽到接近底部 | 监控面板不溢出窗口，发送框始终可见 |

---

## 六、主题系统

### TC-6.1 深浅色切换
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击右上角主题切换开关 | 在深色和浅色模式间切换 |
| 2 | 切换后观察所有 UI 元素 | 颜色正确变化，无残影或错位 |
| 3 | 跟随系统主题时 | 首次启动自动匹配系统深浅色 |

### TC-6.2 主题风格切换
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击全局栏的主题风格下拉 | 显示 6 种风格：默认、浮世绘彩、诗意东方、水墨丹青、桃之夭夭、金风玉露 |
| 2 | 切换为「浮世绘彩」 | 整体配色变为浮世绘风格 |
| 3 | 在浮世绘风格下切换深浅色 | 深色/浅色配色均正确 |
| 4 | 逐一切换所有风格 | 每种风格的深色和浅色均无异常 |

---

## 七、窗口行为

### TC-7.1 窗口缩放
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 拖拽窗口边缘缩小 | 内容自适应缩放，不出现滚动条或截断 |
| 2 | 拖拽窗口边缘放大 | 内容自适应扩展，布局不变形 |
| 3 | 最小化后恢复 | 界面正常显示 |
| 4 | 最大化/还原 | 布局正确适应 |

### TC-7.2 分栏布局
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 单监视器状态 | 监视器占满整个区域 |
| 2 | 打开额外监视器 | 两个面板等宽分栏，中间有蓝色分隔线 |
| 3 | 关闭额外监视器 | 恢复单面板全屏 |

### TC-7.3 窗口位置与大小记忆（v0.5.1 起由 Rust 端 `window.json` 管）
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 启动应用 | 窗口出现在上次的位置与大小，**不是**先闪一下默认几何再跳过去（主窗口是 `visible:false`，恢复完几何才显示） |
| 2 | 把窗口拖到副屏、改成一个不等边尺寸，关闭 | `%APPDATA%\seahi-serial\window.json` 里的 `x`/`y`/`width`/`height` 是退出前的值 |
| 3 | 重新打开 | 位置与大小与上次完全一致（反复开关 3 次，位置不往右下漂移） |
| 4 | 最大化后关闭再打开 | 起来仍是最大化，且**取消最大化后**回到最大化之前的那个普通尺寸（不是全屏尺寸） |
| 5 | 最小化后关闭再打开 | 普通几何不被 `-32000` 之类的哨兵坐标污染，仍回到最小化前的尺寸 |
| 6 | 删除 `window.json` 后打开（模拟老版本升级） | 尺寸回退用 `config.json` 里的 `windowWidth`/`windowHeight`，**位置保持居中**（不跳到左上角） |
| 7 | 把窗口移到副屏后拔掉该显示器，再打开 | 窗口回到可见显示器内（`rect_on_screen` 判定：至少露出 60×40），不会落在看不见的地方 |
| 8 | 临时把 `src/index.html` 的初始化打断（如开头 `throw new Error('x')`） | 窗口**仍然显示**并给出兜底错误页（`revealMainWindow` 与 Rust 4 秒兜底各一道），不会出现"进程在跑但没有窗口" |

---

## 八、配置持久化

### TC-8.1 配置保存/加载
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 设置波特率为 9600，切换主题风格为浮世绘彩 | 配置自动保存 |
| 2 | 关闭程序并重新打开 | 波特率仍为 9600，主题为浮世绘彩 |
| 3 | 添加快捷指令后关闭重开 | 快捷指令保留 |
| 4 | WSL 面板调整分隔线后关闭重开 | 分隔线位置保留 |

### TC-8.2 首次使用引导
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 清除 localStorage 中的 `onboarding_done`，刷新页面 | 引导自动弹出 |
| 2 | 引导共 9 步，每步聚光灯高亮对应元素 | 聚光灯准确、卡片不溢出视口 |
| 3 | 点击「下一步」逐步浏览 | 步骤指示点同步更新 |
| 4 | 点击「跳过」或点击外部区域 | 引导关闭，不再自动弹出 |
| 5 | 最后一步点击「开始使用」 | 引导关闭 |

---

## 九、自动更新

### TC-9.1 更新检测
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 启动程序 | 自动检测 GitHub 最新 Release |
| 2 | 有新版本时 | 全局栏显示「更新」按钮 |
| 3 | 点击「更新」 | 下载并安装更新 |
| 4 | 无新版本时 | 不显示更新按钮 |

---

## 十、异常场景

### TC-10.1 设备热插拔
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 连接串口后拔出设备 | 自动检测断开，显示错误提示 |
| 2 | 开启自动重连后拔出再插入 | 自动恢复连接 |
| 3 | WSL 映射的设备拔出 | 自动解除映射，设备列表更新 |

### TC-10.2 错误处理
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 尝试打开不存在的串口 | 显示错误 toast |
| 2 | 发送数据时串口已断开 | 显示错误 toast |
| 3 | WSL 未安装时打开映射面板 | 提示 WSL 未安装 |
| 4 | usbipd 未安装时尝试映射 | 提示安装 usbipd-win |

### TC-10.3 提交 Issue
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击全局栏「提交 issue」 | 打开浏览器跳转到 GitHub Issues 页面 |

---

## 十一、蓝牙(BLE) 调试

> 前置：电脑蓝牙已开启；附近有可连接的 BLE 设备。详细验证记录见 `doc/BLE_VERIFICATION.md`。

### TC-11.1 扫描与列表
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击顶部「蓝牙调试」 | 进入蓝牙面板，右侧提示「从左侧选择设备…」 |
| 2 | 点击「开始扫描」 | 列表出现设备（名称/地址/地址类型/RSSI/信号条） |
| 3 | 观察图标 | 按类型显示：蓝牙 / Apple / 微软 / iBeacon / Mesh |
| 4 | 观察 iBeacon、Mesh 设备 | 设备名后出现灰色 `(iBeacon)` / `(Mesh)` 提示 |
| 5 | 点击任一设备 | 右侧出现广播内容与 GATT 服务区；展开「广播内容」可见原始字节与按 AD type 切分的段 |

### TC-11.2 连接与状态
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 点击「连接设备」 | 按钮变为「断开设备」，数据日志出现 `[连接中]` / `[连接成功] … 服务 N · 特征 M` |
| 2 | 观察设备列表 | 该设备出现绿色「已连接」提示，且**只有它一台**有该提示 |
| 3 | 等待 3~6 秒 | 详情头部的 RSSI 数值周期刷新 |
| 4 | 切到串口页停留 **≥150 秒**再切回 | 仍为「断开设备」（连接未被系统回收），日志与 GATT 树保留 |
| 5 | 点击「断开设备」 | 按钮恢复「连接设备」，数据日志清空，「已连接」提示消失 |

### TC-11.3 GATT 与读写
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 展开自定义服务（如 `0x…1912`） | 特征行右侧按属性显示动作图标（读取/写入/通知） |
| 2 | 找**同时支持 write 与 write_without_response** 的特征 | **只出现一个**写入图标（不应有两个相同图标） |
| 3 | 点击「读取」图标 | 日志出现 `[读取] <uuid>: <hex>` |
| 4 | 点击「写入」图标 | 弹出「写入特征值」窗口，显示目标特征，输入框自动聚焦 |
| 5 | 输入 `0102` 后回车（文本模式） | 提示「发送成功：0x30313032」，日志有 `[发送] …（文本 · 写响应）` 与 `[发送成功] … 4 字节`，**输入框被清空** |
| 6 | 切到 HEX 模式输入 `01 02` | 发送内容为 `0x0102`（与文本模式不同） |
| 7 | 切换「写响应 / 无响应」 | 日志中的写入方式标记随之变化 |
| 8 | 点击「通知」图标 | 图标变为已启用；设备上行数据时日志出现 `[通知]` |
| 9 | 断开连接 | 通知/订阅图标复位为未启用 |

### TC-11.4 重连与异常
| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 断开后**立即**重连同一设备并发送数据 | 连接与发送均成功（**不应**出现「该对象已经关闭」） |
| 2 | 连接一台随后停止广播的设备 | 该设备仍在列表中并标注「已连接」，右侧详情不空白 |
| 3 | 保持蓝牙面板打开，把设备关机或移出范围 | 约 3 秒内自动变为「连接设备」，提示「设备已断开连接」，日志留一行 `[已断开] 设备侧断开或超出范围（地址）` |
| 4 | 在其它页面把设备关机后再切回蓝牙页 | 显示为未连接，日志留同样一行原因 |
| 5 | 关闭程序 | 设备侧连接被断开（`%TEMP%\seahi-serial-debug.log` 有 `CloseRequested: BLE disconnected`） |
